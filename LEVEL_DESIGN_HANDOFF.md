# 와벨리 동네레벨 시스템 — 디자인 핸드오프 (Claude Design용)

> 이 문서를 Claude Design(또는 디자이너)에게 그대로 전달. **기능 구조(데이터 계약)는 고정, 비주얼은 자유롭게 개선**해서 디자인해줘. 결과는 화면별 HTML/컴포넌트(light+dark, 모바일 우선)로.

---

## 0. 한 줄 요청
와벨리 커뮤니티 "동네레벨" 시스템의 **4개 화면 + 1(선택)**을 와벨리 디자인 언어로 디자인해줘. 레벨·칭호·테두리·뱃지를 유저가 모으고 자랑하는 화면들이야. 당근마켓 온도의 진화형이라고 보면 됨(레벨 + 착용 칭호 + 아바타 테두리 + 사실 뱃지). 전부 **cosmetic(꾸미기 전용)** — 광고 노출·검색 순위에 어떤 이점도 주지 않는 순수 장식이야(그렇게 "보여서도" 안 됨).

## 1. 제품 맥락
- 와벨리(mulgyeol) = **춘천 하이퍼로컬 커뮤니티 + 소상공인 광고** 앱/웹.
- 유저는 글·댓글·좋아요·**매장 GPS 인증방문**으로 XP를 벌고 → 레벨업 → 등급 테두리·칭호·뱃지를 얻음.
- 목적: 재방문·기여를 늘리는 리텐션 장치. 신뢰(인증) 신호와 활동(레벨) 신호를 **구분**해서 보여줘야 함.

## 2. 브랜드 / 디자인 언어 (준수)
- **색:** mulgyeol 브랜드 = 파란 물결. 액센트 = 블루 계열.
- **폰트:** 웹 = Noto Sans KR, 앱 = Pretendard (산세리프, 가독성 우선. 디스플레이 폰트 금지).
- **톤:** 당근st **클린·플랫**. 그라디언트·과한 그림자·네온 금지. "리스트=클린(구분선 최소), 카드=소프트(라운드·은은한 그림자)".
- **모바일 우선** (≤760px는 앱 인터페이스). light/dark 둘 다.
- 실제 구현 시 색·간격·라운드는 토큰(`wavely.tokens.ts` / `index.css`)에서만 — **신규 hex 임의 발명 금지**. 디자인 시안에선 블루 액센트 + 뉴트럴 그레이 기준으로.

## 3. 디자인할 화면
- **A. 아이템 도감** — 테두리·칭호 컬렉션(획득/미획득/한정) + 장착.
- **B. 프로필 레벨 카드** — 마이페이지 상단(아바타 테두리·Lv·진행바·칭호·뱃지).
- **C. 인라인 표시** — 글/댓글의 닉네임 옆(아바타 링 + 칭호 + 등급).
- **D. 레벨업 순간** — 토스트/모달 축하 + 새 획득 아이템 미리보기.
- **E.(선택, v2)** 동네 리그 — 주간 리더보드.

## 4. 데이터 계약 — 반드시 이 슬롯에 맞춰 디자인 (백엔드 확정)
서버가 화면당 1콜로 이걸 줌:

```
get_level_card(user) → {
  nickname,               // 닉네임
  level,                  // 정수 Lv
  tier,                   // 'bronze'|'silver'|'gold'|'platinum'|'diamond'
  xp, xp_into_level, xp_span, progress_pct,   // 진행바(0~100)
  equipped_title,         // 착용 칭호 key (없으면 null)
  equipped_border,        // 착용 테두리 key (없으면 tier로 폴백)
  unlocks: [ { kind, key, earned_at } ]       // 보유 아이템 목록
}
```
- `unlocks[].kind` ∈ `badge` | `title` | `border`
- 착용 변경: `equip_title(key)` / `equip_border(key)` (보유한 것만)
- 잠긴 아이템은 "조건 + 진행"을 함께 보여줘야 함(예: "골드 — 8레벨 남음").

## 5. 콘텐츠 (아이템 전량)

### 5-1. 테두리 (아바타 원형 링)
| key | 이름 | 획득 | 비고 |
|---|---|---|---|
| bronze | 브론즈 | Lv 1–9 | 등급(레벨 파생) |
| silver | 실버 | Lv 10–19 | 등급 |
| gold | 골드 | Lv 20–34 | 등급 |
| platinum | 플래티넘 | Lv 35–49 | 등급 |
| diamond | 다이아 | Lv 50+ | 등급, **미세 애니메이션** |
| founder | 창단 멤버 | 출시 첫 달 가입 | **재획득 불가 = 최고 희소**(별 아이콘) |
| season_sakura | 벚꽃 시즌 | 봄 기간 한정 | 시즌(꽃 아이콘) |
| streak_30 | 개근 이웃 | 연속 출석 30일 | 스트릭(불꽃 아이콘) |
| sponsor_* | 스폰서(매장) | 매장 방문 인증 이벤트 | 매장 한정(매장 아이콘) |

### 5-2. 칭호 (닉네임 옆 텍스트 1개 착용)
새내기 이웃(Lv1) · 동네 지킴이(Lv10) · **○○동 터줏대감**(동 활동 상위) · 맛집 헌터(인증리뷰 30) · 수다쟁이(댓글 300) · 사랑받는 글쟁이(좋아요 500 받음) · 전도사(친구 초대 10) · 동네 반장(Lv20+활동) · **이달의 단골**(매장별 30일 방문 1위, 제로섬)

### 5-3. 뱃지 (사실 기반, 자동 수령)
인증이웃 · 사장님 · 사업자인증 · 단골 · 오래된주민 · 새내기이웃 · 맛집헌터 · 인기글러 · 동네반장

### 5-4. 아이템 상태 (배지/pill로 구분)
- **보유** (착용 가능)
- **잠김** (조건 + 진행률 노출 — 목표 제시)
- **기간·매장 한정** (시즌·스폰서, 마감 시간/조건)
- **재획득 불가(희소)** (창단 멤버 등 — 별도 강조)
- **착용중**

## 6. 화면별 스펙

### A. 아이템 도감
1. **장착 미리보기(상단 카드):** 아바타+현재 테두리, 닉네임, 착용 칭호 칩, 등급명(Lv).
2. **테두리 섹션:** 그리드. 각 카드 = 원형 스와치(링) + 이름 + 조건 + 상태 pill. 특수 테두리엔 코너 아이콘(별/꽃/불꽃/매장).
3. **칭호 섹션:** 그리드. 각 카드 = 칭호 칩 + 조건 + 상태 pill. 착용중은 강조.
4. 잠긴 아이템: 흐리게 + 자물쇠 + "무엇을 하면 얻는지" 진행(예: "8레벨 남음").
5. 탭/토글로 착용(equip). 상태 범례(legend) 하단.

### B. 프로필 레벨 카드
- 아바타(테두리 링) + 닉네임 + 착용 칭호 칩 + **Lv + 진행바(progress_pct%) + 등급명**.
- 그 아래 **뱃지 줄**(획득 뱃지 칩들).
- **신뢰 vs 활동 시각 분리(중요):** 레벨/테두리 = "활동" 신호, 인증·사실뱃지 = "신뢰" 신호. 뱃지엔 근거 노출(예: "검증방문 20곳", "이 동 3년차")해서 레벨 숫자가 신뢰로 오독되지 않게.

### C. 인라인 (글/댓글 헤더)
- 작은 아바타(테두리 링) + 닉네임 + 착용 칭호(작게) + 등급 점(선택).
- **극도로 컴팩트** — 피드 밀도·가독성을 해치면 안 됨. 칭호는 1줄 안에.

### D. 레벨업 순간
- 토스트 또는 가벼운 모달: "**Lv10 달성 — 실버 테두리 획득!**" + 새 테두리/칭호 미리보기 + (선택)자랑하기.
- 과하지 않게, 1~2초 축하감.

### E. 동네 리그 (선택, v2)
- 주간 리더보드: 내 순위 강조, 코호트 20~30명, 상위=승격존/하위=강등존 색 구분, "이번 주 종료까지 남은 시간", 순수 재미 신호임을 명시.

## 7. 크리에이티브 자유 vs 고정
- **자유롭게:** 테두리 시각 스타일(재질감·링 두께·이중링·반짝임), 아이콘 세트, 모션/전환, 색조, 카드 레이아웃 디테일, 범례 표현.
- **고정(바꾸지 말 것):** §4 데이터 슬롯, §5 아이템 라벨·상태, cosmetic-only 원칙(등급이 노출/순위 이점처럼 보이면 안 됨), 화면 목적·핵심 문구.

## 8. 참고 (현재 구조 목업 = 재디자인 기준점)
- 아이템 도감 목업: https://claude.ai/code/artifact/f247e301-74ff-48d2-ba35-eb505e3068be
- 이 목업의 **구조/데이터 배치는 유지**, 비주얼만 와벨리답게 끌어올리면 됨.

## 9. 산출물 형식
- 화면 A~D 각각 HTML/컴포넌트(스트리밍 안전, 인라인 스타일 위주).
- **light/dark 둘 다**, 반응형(모바일 우선 — ≤760px는 하단탭/단일컬럼).
- 자체완결(외부 폰트·CDN 없이, 아이콘은 인라인 SVG). 색은 CSS 변수로.
