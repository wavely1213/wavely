# 와벨리 — 레벨/치장 시스템 "화려도" 상향 요청 (2026-07-27, 사장님 피드백)

> v2 정본 받아 이식 진행 중입니다. 그런데 **치장 시스템 전체가, 특히 배경이 너무 밋밋**합니다. 화려도를 올려주세요.
> 구조·데이터 계약(`unlocks[].kind`, `equip_*`)은 그대로 두고 **비주얼만** 강화 요청입니다.

---

## 1. 문제 진단 (현재 v2 스펙 기준)

배경 9종이 전부 **동일 공식**입니다:
```
.pat { background: var(--patc);        /* 단색 1개 */
       opacity: var(--patop);          /* .16(light) / .24(dark) */
       mask-image: var(--pat); }       /* 선·점 SVG 타일 */
```
- 즉 **단색 + 낮은 투명도 + 선(stroke) 위주 타일** → 9종이 서로 다른 무늬일 뿐 **인상은 거의 같습니다.**
- 애니는 `firework` 1종, 그마저도 "투명도만 호흡 + 7px 드리프트".
- 결과: **"내가 뭘 착용했는지" 자랑이 안 됩니다.** 치장템의 존재 이유(과시·수집욕)가 약해집니다.

## 2. 요청 — 화려도를 "등급/희소도에 비례"하게

전부 화려하게가 아니라 **위로 갈수록 확 화려하게**. 밋밋한 하위가 있어야 상위가 빛납니다.

### 2-1. 배경 ★핵심
현재의 "단색 1레이어"를 **3레이어 구조**로 올려주세요:
1. **베이스 워시** — 단색이 아니라 **그라디언트**(선형/방사/메시). 등급·시즌 색 2~3스톱.
2. **패턴 레이어** — 지금의 마스크 타일 유지(형태 식별용). 단 stroke 위주 → **면(fill)·볼륨** 혼합.
3. **하이라이트 레이어** — 글로우/광택/비네트/스파클 등 "빛" 요소 1개.

- 등급 스케일링: `브론즈=1레이어(현행 수준)` → `실버·골드=2레이어` → `플래티넘·다이아=3레이어 + 미세 모션`.
- **시즌·이벤트 배경(벚꽃·불꽃·창단)은 확실히 튀게.** 지금은 "옅은 선 무늬"라 시즌감이 안 삽니다.
  - 벚꽃: 분홍 그라디언트 + 꽃잎 **실제 형상**(면) + 낙하 모션
  - 불꽃: 발광(글로우) 중심, 어두운 배경일수록 강하게
  - 창단: 금색 광택 + 스파클(희소 최상위답게)
- 투명도 상한을 올려주세요(현행 .16/.24 → 상위 등급은 더 진하게). **단, §3 가독성 가드레일 준수.**

### 2-2. 테두리(링)
- 메탈릭 conic은 좋습니다. **최상위만 더**: 다이아=홀로그램 회전 + 스파클 강화, 창단=금빛 공전 강화.
- 중간 등급(실버·골드)에도 **호버/등장 시 1회 광택 스윕** 정도는 허용.

### 2-3. 칭호 칩
- 레어(★) 금색 그라디언트는 유지 + **미세 shimmer** 1종 추가 제안.
- 최상위 칭호(터줏대감·이달의 단골)는 칩 자체에 작은 글리프/아이콘 허용.

### 2-4. 레벨업·승급 순간
- 지금 스펙(광선 26s 회전 + 컨페티 5)이 **한 번뿐인 순간치고 약합니다.** 등급 승급은 인생에 5번뿐이니 더 크게 — 풀스크린 연출, 등급색 폭발, 새 테두리 클로즈업 등.

## 3. 가드레일 (이건 지켜주세요)
화려하게 하되 아래는 유지 — 안 그러면 커뮤니티가 안 읽힙니다.
1. **가독성**: 히어로/카드 위 텍스트 대비 WCAG AA 유지. 배경이 글자를 이기면 안 됨.
2. **적용 범위 유지**: 배경은 **히어로·도감 카드·리그 헤더만**. 피드 행·댓글엔 절대 금지(현행 §밀도보호 그대로).
3. **`prefers-reduced-motion`**: 모든 모션 정지 경로 필수.
4. **성능**: 앱(RN)은 `<Pattern>`·레이어 비용이 큽니다. **앱은 정적 2레이어까지**, 3레이어·모션은 웹 우선 — 앱 대체 표현을 함께 주세요.
5. **cosmetic-only 원칙 불변**: 화려해져도 노출·순위·추천에 영향 0.

## 4. 산출 요청
- `와벨리 동네레벨 v3.html` — 배경 9종을 **레이어 구조로 재설계**한 목업(등급 스케일링 비교 뷰 포함)
- 각 배경의 **정확한 값**(그라디언트 스톱·투명도·모션 주기), 웹(CSS)과 앱(RN 대체) **양쪽 표기**
- `NEIGHBORHOOD_LEVEL.md §3-4` 갱신

## 5. 참고 — 개발 현황
- 웹: 링 conic 9종·레벨카드·도감 모달 이식 완료. **배경은 아직 미이식** → 지금 재설계해도 손실 없습니다.
- 앱: 레벨카드만(선형 3스톱 링). 도감·배경 미구현.
- DB(RPC) 미배포라 현재 전체 dormant → **비주얼 확정 후 한 번에 붙이는 게 효율적**입니다.
