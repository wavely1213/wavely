# 와벨리 디자인 요청서 — 2026년 8월

이 문서 하나만 읽고 작업할 수 있게 썼다. **추측한 값은 없다. 전부 코드에서 뽑았고 파일:줄을 달았다.**

- 대상: `wavely-web` (소비자웹) — Vite/React, 스타일은 `src/index.css` 한 파일
- 실서비스: https://mulgyeol.kr (모바일은 `m.mulgyeol.kr`로 자동 분기, **같은 빌드**)
- 스탬프 투어 프로토타입: https://mulgyeol.kr/?demo=stamp → 축제 탭 → 스탬프

## 0. 먼저 알아야 할 제약

| | |
|---|---|
| 폰트 | **Noto Sans KR** 고정 ([index.css:19](../wavely-web/src/index.css#L19)). 디스플레이 폰트 미사용 — 가독성 우선. ⚠️ **§1-3 먼저 읽을 것** |
| 색 | **하드코딩 hex 금지.** 아래 토큰만 쓴다. 새 색이 필요하면 토큰을 추가하고 다크모드 짝을 같이 정의 |
| 반응형 | **≤760px = 앱 인터페이스**(하단탭 + FAB, 단일 컬럼) / **PC = 3단**(사이드바 + 본문 + 우측 레일) |
| 다크모드 | 필수. 토큰이 `html[data-theme="dark"]`에서 통째로 교체된다 |
| 우리동네 | PC는 좌우 2단(**목록 440px 고정** + 지도). 이 폭 안에서 안 깨져야 한다 |

> 이 프로젝트는 "서버만 공유, 인터페이스는 플랫폼별로 다름"이 원칙이다.
> 모바일을 PC 축소판으로 만들지 않는다.

---

## 1. 브랜드 컬러 (현행)

### 1-1. 기본 토큰 — [index.css:2~14](../wavely-web/src/index.css#L2)

| 토큰 | 라이트 | 다크 | 쓰임 |
|---|---|---|---|
| `--primary` | `#0EA5E9` | `#38BDF8` | 브랜드 스카이. 버튼·활성칩·링크 |
| `--primary-deep` | `#0E7490` | `#7DD3FC` | 진한 강조. 호버·아이콘 |
| `--primary-soft` | `#EAF7FB` | `#0C2A3F` | 옅은 배경 |
| `--primary-soft2` | `#C6E9F7` | `#123A52` | 테두리·보조 |
| `--ink` | `#18181B` | `#F3F3F5` | 본문 |
| `--ink2` | `#82828C` | `#9B9BA5` | 보조 텍스트 |
| `--ink3` | `#A8A8B0` | `#67676F` | 힌트·캡션 |
| `--line` | `#EAEAEC` | `#232328` | 구분선·테두리 |
| `--bg` / `--page` | `#FFFFFF` / `#F5F5F6` | `#0C0C0E` / `#000000` | 카드 / 페이지 |
| `--field` | `#F1F1F3` | `#1A1A1F` | 입력 배경 |
| `--verify` | `#11B981` | `#11B981` | 인증 초록 |
| `--verify-soft` | `#DDF3EC` | `color-mix(#11B981 17%)` | 인증 배경 |
| `--danger` | `#E5484D` | — | 경고 |
| `--promo-fg` / `--promo-bg` | `#D9730D` / `#FFE7CC` | — | 프로모션 |

**형태 토큰**: `--r-card 16px` / `--r-list 12px` / `--r-chip 999px`
**그림자**: `--sh` `--sh2` `--sh3` (다크에서 값 교체)

### 1-2. 업종색 팔레트 — [app.jsx:165](../wavely-web/src/app.jsx#L165)

지도 핀·카테고리 배지에 쓰인다. **의미색이라 하드코딩이 정상이다**(브랜드색 아님).

| 업종 | hex | | 업종 | hex |
|---|---|---|---|---|
| 전체(기본) | `#0EA5E9` | | 숙박 | `#9B7BFF` |
| 음식점 | `#FF6B4A` | | 여가·오락 | `#FF9F40` |
| 쇼핑 | `#4D96FF` | | 생활서비스 | `#8A94A6` |
| 미용·뷰티 | `#FF6FB5` | | 교육 | `#5FBF73` |
| 의료 | `#00BFA6` | | | |

> ⚠️ `#8A94A6`(생활서비스)는 **업종 미분류 폴백 색과 hex가 완전히 같다** ([app.jsx:1005](../wavely-web/src/app.jsx#L1005)).
> 지금은 "생활서비스"와 "뭔지 모르는 곳"이 지도에서 구분되지 않는다.

### 1-3. 🔴 타이포 — 없는 굵기를 167곳에서 쓰고 있다

**로드 선언** ([index.html:39](../wavely-web/index.html#L39)):
```
Noto+Sans+KR:wght@400;500;700;900
```

**코드가 쓰는 굵기**:

| weight | CSS | JSX | 실제 파일 |
|---|---|---|---|
| 400 | 0 | 1 | ✅ |
| 500 | 2 | 5 | ✅ |
| **600** | **28** | **3** | ❌ 없음 → 700으로 대체 |
| 700 | 73 | 30 | ✅ |
| **800** | **140** | **27** | ❌ 없음 → **900(Black)으로 대체** |
| 900 | 3 | 0 | ✅ |

브라우저에서 실제 로드된 폰트를 확인한 결과 `['400','500','700','900']` 뿐이다.
(`document.fonts.check()`는 폴백까지 true로 답하므로 믿으면 안 된다.)

**즉 ExtraBold(800)로 설계한 167곳이 전부 Black(900)으로 렌더되고 있다.** 제목·라벨·강조가
의도보다 굵다. 지금 화면 캡처를 볼 때 이 점을 감안해 달라.

**요청**: 굵기 체계를 정해달라. 둘 중 하나다.
- (A) `600;800`을 로드 목록에 추가 — 의도대로 나오지만 폰트 용량이 는다
- (B) 코드를 로드된 굵기(400/500/700/900)로 정리 — 용량 그대로, 대신 위계를 다시 짜야 한다

**(B)를 권한다.** 굵기 6단계는 어차피 과하다. 4단계로 위계가 서면 더 깔끔하다.

---

## 2. 🔴 지금 고쳐야 할 것 — 지도 마커

**이번 요청의 핵심이다.** 사용자 제보가 반복적으로 나온 곳이고, 근본 원인은 *색으로 구분하려 한 것*이다.

### 2-1. 현재 마커 5종 (실제 값)

| 마커 | 크기 | 형태 | 색 | 위치 |
|---|---|---|---|---|
| `.nv-mk` 매장 | 24px | **원** + 흰테 2.5px | 업종색 (기본 `#0EA5E9`) | [:744](../wavely-web/src/index.css#L744) |
| `.nv-mk.ad` 광고 | 27px | 원 + **파란 후광** `0 0 0 3px rgba(14,165,233,.3)` | 업종색 | [:745](../wavely-web/src/index.css#L745) |
| `.nv-mk.vf` 인증 | 24px | 원 + 초록테 | 업종색 + `--verify` 테두리 | [:746](../wavely-web/src/index.css#L746) |
| `.nv-cluster` 묶음 | 32/40/48/56px | 원 + 흰테 2.5px | 그라데이션 `#38BDF8→#0E7490` | [:748](../wavely-web/src/index.css#L748) |
| `.nv-myloc` 내 위치 | 14px | 원 + 흰테 3px + **맥동** | `--primary-deep` | [:747](../wavely-web/src/index.css#L747) |

클러스터 지름은 개수로 결정: `n<10 → 32px`, `<30 → 40`, `<80 → 48`, 그 외 `56`.

### 2-2. 문제 — 전부 "흰 테두리 두른 파란 원"이다

색으로 가르려는 시도 자체가 막혀 있다. 지도 표면 위 파랑 계열만 5종이다:

| | hex |
|---|---|
| 업종 '전체' 기본 | `#0EA5E9` |
| 업종 '쇼핑' | `#4D96FF` |
| 클러스터 그라데이션 시작 | `#38BDF8` |
| 클러스터 그라데이션 끝 | `#0E7490` |
| **내 위치** | **`#0E7490`** ← 클러스터 끝색과 **hex 완전 동일** |

마지막 줄이 특히 나쁘다. 내 위치를 `--primary-deep`으로 잡았는데 그게 클러스터 그라데이션의
어두운 끝과 같은 값이다. *(2026-08-02에 내가 넣은 것이다.)*

거기에 **광고 핀의 파란 후광**(`0 0 0 3px rgba(14,165,233,.3)`)과 **내 위치의 맥동 후광**은
시각 언어가 거의 같다.

즉 **남은 색이 없다.** 다른 차원(형태·아이콘·움직임)으로 갈라야 한다.

### 2-3. 방향 — 색 4개 + 아이콘 (2026-08-03 결정)

실데이터 13,089곳의 업종 분포를 뽑아보니 **음식점 하나가 34.5%**이고 나머지는 4~11%로 고만고만하다.
9색을 쓸 근거가 없다.

| 업종 | 개수 | 비중 |
|---|---|---|
| 음식점 | 4,521 | 34.5% |
| 기타 | 2,365 | 18.1% |
| 쇼핑 | 1,468 | 11.2% |
| 미용·뷰티 | 1,069 | 8.2% |
| 생활서비스 | 979 | 7.5% |
| 교육 | 772 | 5.9% |
| 여가·오락 | 725 | 5.5% |
| 숙박 | 601 | 4.6% |
| 의료 | 589 | 4.5% |

**색은 "대충 어떤 부류"를, 아이콘은 "정확히 뭔지"를 나눠 맡는다.** 색만 9개면 범례를 외워야 하지만
아이콘이 있으면 처음 온 사람도 바로 안다.

| 색 | 묶음 | 개수 | 아이콘 예 |
|---|---|---|---|
| 1 | 먹기 (음식점) | 4,521 · 35% | 수저, 커피잔 |
| 2 | 쇼핑·생활 (쇼핑+생활서비스) | 2,447 · 19% | 쇼핑백, 렌치 |
| 3 | 놀기·배움·자기 (여가+교육+숙박) | 2,098 · 16% | 마이크, 책, 침대 |
| 4 | 건강·미용 (의료+미용뷰티) | 1,658 · 13% | 십자, 가위 |

의료와 미용을 한 색으로 묶는 게 어색해 보이지만 **아이콘이 다르니 안 헷갈린다.** 색은 "몸에 관한 것"
정도만 알려주면 된다 — 이게 아이콘 방식의 이점이다.

**크기 산식** (지금보다 4px만 커진다):
```
지금   원형 24px − 흰테 2.5px×2 = 안쪽 19px  (아이콘 없음)
제안   물방울 28px → 머리 24px − 흰테 = 안쪽 20px → 아이콘 13px
```
아이콘이 12px 밑으로 가면 글리프가 뭉개진다(구글 지도도 24dp 핀에 12~14dp 아이콘). 그게 하한선이다.

**아이콘을 넣지 말 것 두 개**:
- **클러스터** — 숫자가 이미 정보다. 아이콘까지 넣으면 읽을 게 둘
- **내 위치** — 무늬 없는 납작한 원 그대로. 장소가 전부 "물방울+아이콘"이 되면 **아무것도 없는 원 하나만 나**가 되어 구분이 더 선명해진다

**미결** — '기타' 2,365곳(18%)을 5번째 회색으로 둘지, 아이콘 없는 무채색 점으로 낮출지.
후자를 권한다. 정체 모르는 곳이 눈에 띄면 지도만 지저분해진다.

> ⚠️ **분류 체계가 두 개다.** 지도 핀 색은 `CATS`(한글 8업종, [app.jsx:165](../wavely-web/src/app.jsx#L165))를 보고,
> 목록 필터는 `PLACE_CATS`(영문 5키) + `PLACE_CATS_MORE`(13키)를 본다. 서로 매핑이 없어서
> **지금 구조로는 범례를 만들 수 없다.** 4분류를 정할 때 이 둘을 하나로 합칠지도 같이 정해야 한다.

### 2-4. 이미 답이 코드 안에 있다

축제 지도의 마커는 **물방울 모양**이다 — [index.css:1177](../wavely-web/src/index.css#L1177)

```css
.fest-mk { width:26px; height:26px;
  border-radius:50% 50% 50% 3px;   /* ← 한 귀퉁이만 뾰족 */
  transform:rotate(45deg); }
```

**같은 앱인데 지도 두 개가 서로 다른 언어를 쓰고 있다.** 우리동네는 원, 축제는 물방울.

### 2-5. 요청 — 마커 체계 재설계

**산출물: 마커 5종 + 선택 상태 + 아이콘 세트.** 제약은 §2-3에 다 적었다(4색 · 물방울 28px · 아이콘 13px).

원칙(바꿔도 좋으니 근거만 달라):

1. **장소 = 물방울**(꼬리가 지면을 가리킴) + 업종 아이콘 — 매장·광고·인증
2. **지면 위의 나 = 납작한 원**(꼬리·아이콘 없음) — 내 위치
3. **위계 3단**: 광고 > 인증·등록 > 일반. 지금은 24/27px로 **3px 차이뿐이라 위계가 안 읽힌다**
4. **클러스터**는 장소가 아니라 묶음이므로 다른 형태여도 좋다

아직 없어서 같이 그려야 하는 것:
- **선택된 마커**(`selectedId`) 강조 — 지금 아무 표시가 없다
- 마커 탭 시 뜨는 미리보기 카드(`.web-mappreview`)와의 관계
- **범례**를 지도에 둘지 (4색이면 둘 만하다. 9색일 땐 불가능했다)

**참고 — 밀도는 이미 코드로 해결했다.** 클러스터 셀을 줌에 반비례시키고 단독 셀을 이웃으로 흡수해서, 기본 줌 기준 마커 60개 → 22개, 겹침 984쌍 → 1쌍이 됐다. **디자인에서 밀도를 걱정할 필요는 없다.** 형태와 위계만 봐 달라.

---

## 3. 🔴 접근성 — 색 대비 미달 (실측)

WCAG 기준 계산 결과다. **본문 4.5:1, 큰 글씨·UI 3:1**이 기준선.

| 조합 | 비율 | 판정 |
|---|---|---|
| **흰 글자 / `--primary`** | **2.77:1** | ❌ 미달 |
| **`--ink3` / 흰 배경** | **2.36:1** | ❌ 미달 |
| `--ink2` / 흰 배경 | 3.80:1 | ⚠️ 큰 글씨만 |
| **`--verify` / `--verify-soft`** | **2.19:1** | ❌ 미달 |
| 흰 글자 / `--fest` | 3.01:1 | ⚠️ 큰 글씨만 |
| `--primary-deep` / 흰 | 5.36:1 | ✅ 통과 |
| `--ink` / 흰 | 17.72:1 | ✅ 통과 |

영향 범위가 넓다:

- **흰 글자 on `--primary`** — 글쓰기 버튼, 모든 활성 칩(`.web-chip.is-active`, `.web-catchip.is-active`, `.web-fchip.is-active`), 광고 CTA, 댓글 등록 등 **8곳 이상**
- **`--ink3`** — 코드에서 **72회** 사용. 캡션·힌트 대부분
- **`--verify` on `--verify-soft`** — 마이페이지 신뢰 뱃지 칩. *이건 2026-08-02에 내가 만들면서 생긴 것이다*

### 요청
`--primary`를 밝기만 낮춰 4.5:1을 맞출지, 아니면 **버튼용 딥 톤을 따로 둘지**(예: `--primary-on-fill`) 정해달라. 브랜드 스카이의 인상을 지키면서 대비를 확보하는 쪽을 원한다. `--ink3`도 어디까지 어둡게 할 수 있는지 봐 달라.

---

## 4. 축제탭 — 2026 춘천막국수닭갈비축제

### 4-1. 축제 전용 색 — [index.css:947](../wavely-web/src/index.css#L947)

`.fest` 스코프 안에서만 적용된다. **앱 나머지는 스카이 그대로.**

| 토큰 | 라이트 | 다크 | 쓰임 |
|---|---|---|---|
| `--fest` | `#F26D1F` | `#FF8A45` | 축제 오렌지 |
| `--fest-deep` | `#C7500F` | `#FFA96F` | 진한 강조 |
| `--fest-soft` | `#FDECE0` | `#2A1710` | 옅은 배경 |
| `--fest-line` | `#F7C9A8` | `#4A2818` | 테두리 |
| `--dak` 닭갈비 | `#E8461E` | — | 팀 색 |
| `--mak` 막국수 | `#2A9D8F` | — | 팀 색 |
| `--show` 공연 | `#0EA5E9` | — | 프로그램 종류 |

> 오렌지는 **히어로·배너·아이콘·타임라인 강조에만** 쓴다. 배경은 앱과 같은 중립색이다.

### 4-2. 축제 정체성

포스터가 **"막 vs 닭" 대결 구도**다. 막국수 캐릭터와 닭 캐릭터가 VS로 맞붙고, 카피가 *"묻고 싶다!! 과연 승자는 존재하는가?!"*. 축제 기간 10.14(수)–18(일), 장소 공지천 산책로 일대.

**이 대결 구도를 축제탭 전반에 더 쓸 수 있는지 봐 달라.** 지금은 스탬프 투어에서만 팀 색으로 쓰고 있다.

### 4-3. 세부탭 구조 (2026-08-02 신설)

`안내`(기본) / `타임테이블` / `매장` / *(스탬프 — 데모에서만)*
히어로·빠른버튼(전화·길찾기·인스타·공식사이트)·축제 이야기 게시판·푸터는 **탭과 무관하게 항상** 노출.

**문제**: 타임테이블·매장은 공식 미공개라 빈 상태다. 탭 고유 콘텐츠가 각각 605px·541px(0.7·0.6화면)로, 안내 탭(1074px)의 절반이다. 빈 상태 디자인이 이 탭들의 얼굴이 된다.

### 4-4. 빈 상태 — 여기가 약하다

현재 타임테이블 탭:
- 아이콘 + "프로그램 준비 중이에요" + "확정되는 대로 시간표로 올려드릴게요"
- **유령 슬롯 3개**(`.fest-ghost`) — 로딩 셔머가 아니라 '미공개'라 정적
- 하단에 "공개되면 알림 받기" 버튼 (테이블 적용 시 활성)

**요청**: 빈 상태를 "기다리세요"가 아니라 **"곧 여기 이런 게 채워진다"를 보여주는 화면**으로 만들어 달라. 축제까지 D-72라 이 화면이 두 달 넘게 노출된다. 제안서에도 들어간다.

### 4-5. 스탬프 투어 (프로토타입)

**부스·매장이 확정 전이라 데이터가 전부 예시다.** `?demo=stamp`일 때만 렌더된다.

구조 — [index.css:1291~](../wavely-web/src/index.css#L1291):
- **구불구불한 길** 위에 **54px 원형 도장** 8개. 좌표계는 `x=%`(가로) · `y=밴드`(세로, 1밴드 78px)
- 길은 SVG 점선(`--fest-line`, 3px, dash `1 9`)
- 팀 색으로 갈림: 막국수 청록 / 닭갈비 적 / 공연·체험 오렌지
- 찍히면 채워지고 **-7도 기울어** 도장 눌린 느낌
- PC에서는 `max-width 420px` 가운데 정렬(폭이 벌어지면 간격만 늘어남)

인증 2종:
| 방식 | 흐름 |
|---|---|
| **부스** | 방문자가 QR을 띄움 → **운영자가 스캔** → 찍힘 |
| **인증샷** | 드론쇼·공연처럼 부스 없는 프로그램. 촬영 위치·시각으로 판정 |

> QR은 형태만 재현한 **예시**라 실제로 스캔되지 않는다. "예시 · 스캔되지 않아요" 라벨이 코드 위에 붙어 있다.

**요청**:
1. 도장 아이콘이 지금 범용 아이콘(가게/카메라)이다. **막국수·닭갈비 캐릭터를 쓸 수 있는지** — 포스터 캐릭터 활용 가능 여부는 주최측 승인 필요
2. 완주 시 연출(지금 없음)
3. **보상은 비워뒀다** — 주최측과 정할 사항. 경품/굿즈/와벨리 한정 뱃지 중 선택지를 시각화해주면 제안서에서 고르게 할 수 있다

---

## 5. 그 외 손봐야 할 곳

### 5-1. 마이페이지 레벨 카드
신뢰 뱃지를 전폭 카드 → 칩으로 압축했다(PC 341px → 57px). 대신 **근거문구("사업자등록 확인 완료" 등)가 화면에서 사라지고 `title` 속성에만 남는다.** 정보를 되살릴 방법이 필요하다 — 칩 탭 시 펼치기? 툴팁?

### 5-2. 반응형 취약 유형
PC 440px 목록 패널에서 **거리 필터(3km·5km)가 잘려 클릭 불가**였다(2026-08-02 수정). 원인은 `flex-wrap`이 모바일 미디어쿼리에만 걸려 있던 것. **같은 유형이 더 있을 수 있다** — 좁은 패널 안에 가로로 늘어서는 요소를 전수 점검해달라.

### 5-3. 칩 오버플로
칩바가 440px 패널에서 항상 넘친다. 지금은 스크롤바를 숨기고 오른쪽에 마스크 페이드를 뒀다. **"더 있다"를 알리는 더 나은 방법**이 있으면 제안해달라.

### 5-4. 빈 상태가 22곳인데 품질이 제각각
전수 조사 결과 빈 상태 화면이 **22곳**이다. 그중:
- 공용 컴포넌트(`WebEmpty`)를 안 쓰고 **인라인 한 줄로 때운 곳 5곳**
- 설명(sub)이 **빈 문자열인 곳 1곳**
- `WebEmpty`가 액션 버튼을 지원하는데 **14곳 중 4곳만** 사용

**요청**: 빈 상태 **한 벌의 규격**을 정해달라(아이콘·제목·설명·액션의 유무 기준). 지금은 화면마다 톤이 다르다.

### 5-5. 죽은 토큰 정리
참조 0회인 토큰이 있다 — `--promo-bg` / `--promo-fg` / `--show`. `--card`는 `--bg`와 값이 항상 같아 실질 중복.
다크 대응이 없는 토큰도 있다 — `--danger` `--g1/--g2` `--sk1/--sk2` `--onlight`.
**정리할지, 다크값을 채울지** 정해달라.

---

## 6. 산출물 요청

우선순위 순:

1. **지도 마커 체계 + 업종 아이콘 세트** — 마커 5종 + 선택 상태 + 4분류 아이콘. 제약은 §2-3에 확정돼 있다 (§2)
2. **타이포 굵기 체계** — 800이 167곳에서 900으로 렌더 중. 4단계로 정리 권장 (§1-3)
3. **색 대비 교정안** — `--primary` 위 흰 글자, `--ink3`, `--verify` 칩 (§3)
4. **축제 빈 상태** — 타임테이블·매장 탭. D-72 동안 노출되고 제안서에도 들어간다 (§4-4)
5. **빈 상태 규격 한 벌** — 22곳이 제각각이다 (§5-4)
6. **스탬프 도장 아이콘 + 완주 연출** (§4-5)
7. 레벨 뱃지 근거문구 복원 방식 (§5-1)

1·2·3은 서로 얽혀 있다(마커에 아이콘이 들어가면 굵기·대비도 같이 정해진다). 한 번에 봐주는 게 좋다.

형식은 지금까지처럼 **프로토타입 HTML**이 가장 좋다. 그게 픽셀 스펙이 된다.

---

## 부록 — 참고 링크

| | |
|---|---|
| 실서비스 | https://mulgyeol.kr |
| 스탬프 데모 | https://mulgyeol.kr/?demo=stamp → 축제 → 스탬프 |
| 축제 공식 | https://mdfestival26.imweb.me |
| 스타일 원본 | `wavely-web/src/index.css` |
| 마크업 원본 | `wavely-web/src/app.jsx` |
