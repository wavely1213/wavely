# 파장 WAVELENGTH — 디자인 가이드 · 남은 작업

이 문서는 두 부분이다.

- **1장 확정 규칙** — 이미 정해졌고, 앞으로 어떤 작업을 하든 지켜야 하는 것. 바꾸려면 근거를 남기고 바꾼다.
- **2장 남은 작업** — 지금 해야 하는 디자인 작업 전부. 우선순위 · 손댈 곳 · 완료 기준까지.

기준선 측정치는 5장에 있다. 작업 후 그 수치를 다시 재서 비교한다.

관련 문서: 시스템·데이터 구조는 [`README.md`](./README.md).

---

## 1. 확정 규칙

### 1.1 색은 의미에 1:1로 고정

화면에 탄이 40발 깔려도 읽혀야 한다. 색을 장식으로 쓰지 않는다.

| 토큰 | 라이트 / 다크 | 무엇에만 쓰는가 |
|---|---|---|
| `--c-signal` | `#d97757` | 플레이어 기체 · 아군 탄 · 추진기 발광 |
| `--c-drift` | `#5d8fc2` / `#6a9bcc` | 군체 · 적 탄 · 보스 · 육각 차폐 |
| `--c-moss` | `#788c5d` / `#94ab72` | 코어 · 보급품 등 **회수 가능한 것** |
| `--c-dust` | `#b0aea5` | 계기 · 배경 파형 · 중립 표식 |
| `--c-fg` / `--c-field` | 잉크 / 지면 | 글자 · 외곽선 / 플레이필드 바닥 |

- 새 색을 만들지 않는다. 필요하면 `shade(base, amt)`로 기존 토큰에서 파생한다.
- 텍스트에는 `-t` 접미 토큰(`--c-signal-t`, `--c-drift-t`)을 쓴다. 면 색을 그대로 글자에 쓰면 라이트에서 대비가 깨진다.
- 오렌지↔블루 조합은 적록색약에서도 안전한 축이다. 이 축을 유지하는 한 색맹 대응은 따로 필요 없다.
  다만 **색만으로 구분하지 않는다** — 아군 탄은 세로 막대, 적 탄은 원처럼 형태도 같이 다르게 한다.

### 1.2 두 테마를 같은 밀도로 설계

다크는 잉크 필드, **라이트는 종이 필드**로 군체가 뒤집힌다. 라이트는 다크의 반전이 아니라 별도 디자인이다.

- `prefers-color-scheme`(OS)과 `:root[data-theme]`(뷰어 토글)을 **양방향 모두** 대응한다. 컴포넌트는 항상 토큰을 거쳐 스타일링하고, 미디어쿼리 안에서 직접 색을 쓰지 않는다.
- 새 화면을 만들면 두 테마 스크린샷을 모두 확인한다. 하나만 보고 넘어가지 않는다.

### 1.3 캔버스 렌더 3원칙

작업하며 실제로 밟은 함정에서 나온 규칙이다. 새 아트를 붙일 때 반복해서 걸린다.

**① 구조선은 화면 좌표, 면은 모델 좌표**

외곽선·이음새 굵기를 모델 좌표(`lineWidth = 1.1`)로 두면 5배 확대에서 테두리도 5배가 되어 스티커처럼 보인다.

```js
const lw = 1.3 / sc;          // 구조선: 배율과 무관하게 화면상 굵기 고정
g.lineWidth = lw;             // 외곽선
g.lineWidth = lw * .85;       // 패널 이음새
// 그라디언트 · 베벨 같은 '면' 효과만 모델 좌표를 따른다
```

**② 하드웨어 색에 `C.fg`를 쓰지 않는다**

`C.fg`는 테마에 따라 뒤집힌다. 노즐·포드 같은 금속 부품에 쓰면 다크에서 새하얘진다.

```js
g.fillStyle = shade(base, -.66);   // O — 도장색 파생
g.fillStyle = shade(C.fg, -.05);   // X — 다크에서 흰색
```

**③ 배율별 LOD를 반드시 둔다**

같은 함수가 게임 중(1배)과 프리뷰(2.9~5배)에 모두 쓰인다. 큰 쪽 기준으로 그리면 작을 때 뭉개지고, 작은 쪽 기준으로 그리면 클 때 빈약하다.

```js
const hi = sc >= 1;
if (!hi) { /* 단색 + 외곽선으로 끝 */ return; }
/* 이하 풀디테일 */
```

**보조 규칙**

- 타원을 얼굴 위치에 두지 않는다. 조종석을 타원으로 두었더니 확대 시 얼굴처럼 읽혀 각진 캐노피로 바꿨다.
- 세부 좌표는 그리기 코드가 아니라 데이터에 둔다(`HULL`, `DETAIL`). 새 기체는 두 줄 추가로 같은 마감을 받는다.
- 파티클·흔들림은 `REDUCED`(`prefers-reduced-motion`)를 반드시 확인한다.

### 1.4 타이포는 역할로 나눈다

| 역할 | 토큰 | 쓰는 곳 |
|---|---|---|
| 계기 · UI | `--font-ui` | HUD, 메뉴, 버튼, 라벨 |
| 내러티브 | `--font-story` | 스토리 대사, 기체·탑승자 소개문 |
| 수치 | `--font-num` | 점수, 코어, 스탯, 형식번호 (`tabular-nums` 필수) |

계기와 내러티브를 서로 침범시키지 않는다. 대사에 산세리프를 쓰면 관제 로그처럼 읽히고, HUD에 세리프를 쓰면 계기판이 무너진다.

> ⚠️ 현재 `Poppins`/`Lora`는 **로드되지 않는다.** 웹폰트를 링크하지 않았고 둘 다 시스템 폰트가 아니다.
> 실제로는 폴백(Century Gothic / Trebuchet / Georgia)이 뜬다. 2장 P2-10 참조.

### 1.5 레이아웃

- 세로 2:3 스코프 하나. 모든 메뉴는 그 **안에서** 열린다. 웹페이지에 게임을 끼워넣은 형태로 만들지 않는다.
- 가로 스크롤 금지. 넓은 내용은 자기 컨테이너에서 `overflow-x: auto`.
- 필드 논리 해상도는 480×720 고정. 좌표를 하드코딩할 때 이 기준을 쓴다.

### 1.6 성능 예산

- 외부 요청 **0**을 유지한다. CDN·폰트·이미지 링크 금지, 전부 인라인 또는 코드 생성.
- 풀디테일 패스는 화면당 소수 오브젝트에만(플레이어 1 + 보스 1 + 카드 4). 군체는 수가 많으므로 경량 패스를 별도로 유지한다.
- 파일 크기 기준선 101KB. 폰트를 인라인하면 이 숫자가 크게 뛴다 — 결정 전에 P2-10을 읽는다.

---

## 2. 남은 작업

> **진행 상황 (2026-07-29)** — 완료되어 코드에 반영된 항목:
> P0 전부(군체 마감 · 구역별 배경 · 라이트 대비 · 터치 타겟),
> P1 전부(타격감 · 폭탄 충격파 · 보스 등장·격파 · 무기 레벨별 탄 · 구역 전환),
> P2-11 결과 화면 · P2-12 스토리 화자 · P2-13 격납고 비교 · P2-14 2톤 도장 · P2-15 사운드,
> A-1 무한 모드 곡선 · A-2 스토리 순차 출력.
>
> **남은 것은 두 가지뿐이고 둘 다 결정이 필요하다** — P2-10 폰트 인라인, P3-16 탑승자 시각화.
> (P3-17 BGM · P3-18 파비콘은 선택 사항.)
>
> 재측정치(2026-07-29): 최대 부하 프레임 중앙값 16.7ms / p95 17.2ms(기준선 유지),
> 라이트 `moss-t` 대비 3.49 → **5.92**, 모바일 터치 타겟 44px 충족, 파일 126KB.

각 항목은 **현재 → 목표 → 손댈 곳 → 완료 기준** 순이다.

### P0 — 품질 격차가 눈에 보이는 것

#### P0-1. 군체 마감이 기체에 뒤처진다 ★ 가장 먼저

- **현재** — 기체는 금속 그라디언트·베벨·이음새·노즐 발광까지 갔는데, 군체 4종은 단색 폴리곤 + 표식 몇 개다. 한 화면에 같이 있으면 격차가 바로 보인다.
- **목표** — 군체도 조립품으로 읽히게. 다만 **수가 많으므로 기체와 같은 비용을 쓸 수 없다.** 저비용 버전을 따로 설계한다.
  - 세로 2단 그라디언트(위 밝게/아래 어둡게) — 그라디언트 객체는 종류별 1회 생성 후 재사용
  - 상단 하이라이트 1줄 + 하단 그늘 1줄 (베벨 전체 대신 선 2개)
  - 피격 시 흰 플래시 2프레임 (지금은 파티클만이라 맞았는지 불확실)
  - 파괴 직전 상태에 노출 배선/불꽃 1점
- **손댈 곳** — `drawEnemy()`. 그라디언트는 모듈 스코프에 캐시하고 테마 변경 시 무효화(`readColors` 훅).
- **완료 기준** — 최대 부하(군체 17 + 보스 + 탄 25)에서 프레임 시간 중앙값이 기준선(16.7ms) 대비 +2ms 이내. 기체와 나란히 놓은 스크린샷에서 마감 급이 같아 보일 것.

#### P0-2. 5개 구역의 배경이 전부 똑같다

- **현재** — `drawBackdrop()`이 `G.stage`를 참조하지 않는다(확인함). 정적 지대와 근원이 시각적으로 구분되지 않아, 진행감이 숫자로만 존재한다.
- **목표** — 구역마다 배경이 확실히 다르되, **탄 가독성을 해치지 않는 선**에서. 각 구역의 이름이 이미 방향을 정해준다.

  | 구역 | 배경 방향 |
  |---|---|
  | 1 정적 지대 | 지금 그대로 — 기준값. 파형 진폭 최소 |
  | 2 간섭 구역 | 파형 2겹이 서로 어긋나며 간섭 무늬. 리벳 열 밀도 ↑ |
  | 3 반향의 벽 | 지나간 파형이 지연되어 한 번 더 흐름(잔상 레이어) |
  | 4 백색 소음 | 파형이 무너져 미세 노이즈 필드로. 격벽 이음새 사라짐 |
  | 5 근원 | 아래에서 위로 역류. 화면 상단에 광원 하나 |
  | 6+ 잔향 | 4·5를 섞고 색상만 서서히 탈색 |

- **손댈 곳** — `drawBackdrop()`에 `G.stage` 분기. 구역별 파라미터(진폭·겹수·유속·노이즈량·이음새 유무)를 `BACKDROP[stage]` 데이터로 빼고 그리기 코드는 하나로.
- **완료 기준** — 5구역 스크린샷을 나란히 놨을 때 이름만 보고 매칭 가능. 각 구역에서 적 탄이 배경에 묻히지 않을 것(다크·라이트 모두).

#### P0-3. 라이트 테마에서 `moss` 소형 텍스트 대비 미달

- **현재** — 측정치 **3.49**. 큰 글씨(19px 코어 잔액)는 3.0 기준을 넘어 통과하지만, 10~11px로 쓰이는 곳(`.stat.up b`, `.row .st b`)은 본문 기준 4.5에 미달한다.
- **목표** — 라이트 전용 텍스트 토큰 `--c-moss-t`를 추가해 소형 텍스트에만 적용. 면(코어 표식·보급품)은 지금 색 유지 — 그건 대비 기준 대상이 아니다.
- **손댈 곳** — `:root` / `:root[data-theme="light"]`에 `--c-moss-t: #5a6b45` 정도 추가 후, `.stat.up b`·`.row .st b`·`.log` 계열이 그 토큰을 쓰게.
- **완료 기준** — 라이트에서 4.5 이상. 다크 값(7.31)은 유지.

#### P0-4. 모바일 터치 타겟이 작다

- **현재** — 390px 폭 실측: `.tab` 66×**34**, `.buy` 55×**33**, `.iconbtn` 69×**25**. 권장 44×44 미달.
- **목표** — 시각적 크기는 그대로 두고 **히트 영역만** 44px로 넓힌다. 촘촘한 계기판 느낌을 잃지 않기 위해서다.
- **손댈 곳** — 해당 클래스에 `min-height: 44px`(탭·버튼) 또는 `::after`로 투명 확장 영역. 인접 타겟 간 최소 8px 간격 확보.
- **완료 기준** — 390px에서 모든 상호작용 요소가 44×44 이상, 겹침 없음. 데스크톱 레이아웃 변화 없을 것.

---

### P1 — 조작 감각

#### P1-5. 타격감이 약하다

- **현재** — 처치 시 파티클 + 화면 흔들림만. 적을 맞히는 순간의 확인이 부족하고, 처치가 "사라짐"으로 느껴진다.
- **목표**
  - **히트스톱** — 처치 시 40~60ms 정지. 보스 격파는 140ms. 중독성에 가장 크게 기여하는 한 가지다.
  - **피격 플래시** — 맞은 군체를 2프레임 흰색으로 덮는다.
  - **파편** — 정사각 파티클 대신 회전하는 금속 조각(가는 직사각형 + 각속도). 기계화 톤과 직결.
  - **콤보 상승** — 배율이 오르는 순간에만 HUD 수치가 튄다(항상 움직이면 시끄럽다).
- **손댈 곳** — `damageEnemy()`, `burst()`, 메인 루프(`frame()`)에 히트스톱 타이머. `REDUCED`일 때 히트스톱은 유지(모션이 아니라 타이밍), 파편 수만 감소.
- **완료 기준** — 히트스톱 중에도 입력이 씹히지 않을 것. 연속 처치 시 정지가 누적되지 않도록 상한(프레임당 1회).

#### P1-6. 폭탄 연출이 시스템 비중에 비해 초라하다

- **현재** — 플래시 + 흔들림 + 파티클. 최후 수단인데 최후 수단처럼 보이지 않는다.
- **목표** — 확장하는 육각 충격파 링(세계관의 육각 차폐와 같은 어휘), 링이 지나간 자리의 적 탄이 순차적으로 소거, 0.3초 저속.
- **손댈 곳** — `useBomb()` + 링 상태를 `G`에 추가하고 `draw()`에서 렌더.
- **완료 기준** — 링 통과와 탄 소거 타이밍이 시각적으로 일치할 것(지금은 즉시 비운다).

#### P1-7. 보스의 등장과 격파에 연출이 없다

- **현재** — 위에서 그냥 내려오고, 죽으면 파티클이 터진다.
- **목표** — 등장: 경고 표시(`AX 증폭 코어 · 접근`) + 차폐층이 한 겹씩 전개. 격파: 차폐 붕괴 → 장갑 분리 → 코어 폭발 3단.
- **손댈 곳** — `startBoss()` / `killBoss()`에 연출 단계 타이머.
- **완료 기준** — 등장 연출 중 피격 불가(무적) 처리로 억울한 죽음이 없을 것.

#### P1-8. 무기 레벨이 올라도 탄이 똑같이 생겼다

- **현재** — 레벨 1~4가 동일한 막대. 관통(5)만 굵다. 강화의 보상감이 시각적으로 없다.
- **목표** — 레벨마다 탄 길이·코어 밝기·발사 섬광 크기를 단계적으로. 5레벨 관통탄은 잔광을 남긴다.
- **손댈 곳** — `B()`에 레벨 정보 전달, `draw()`의 아군 탄 분기. 발사 위치에 머즐 플래시 추가.
- **완료 기준** — 레벨 1과 5를 나란히 놓은 스크린샷에서 차이가 즉시 보일 것.

#### P1-9. 구역 전환이 텍스트 한 줄이다

- **현재** — 「구역 정리 완료」 페이드.
- **목표** — 다음 구역 이름·형식 표기가 계기판에 갱신되는 연출. 배경이 다음 구역 파라미터로 전이(P0-2와 묶어서).
- **손댈 곳** — `G.phase === "clear"` 렌더 분기, `nextStage()`.
- **완료 기준** — 전환 중 입력이 잠기지 않을 것(폭탄·이동 유지).

---

### P2 — 완성도

#### P2-10. 타이포가 의도대로 뜨지 않는다 ★ 결정 필요

- **현재** — `Poppins`/`Lora`를 지정했지만 **로드되지 않는다.** 웹폰트를 링크하지 않았고(외부 요청 0 원칙 + 아티팩트 CSP가 폰트 CDN 차단), 둘 다 시스템 폰트가 아니다. 실제로는 Century Gothic / Trebuchet / Georgia 폴백이 뜬다.
- **선택지**

  | 안 | 내용 | 비용 | 결과 |
  |---|---|---|---|
  | A | **라틴 서브셋만 `@font-face` data URI 인라인** | woff2 기준 +40~80KB | 워드마크·`STAGE`·형식번호 등 디스플레이가 의도대로. 한글은 시스템 폰트 유지 |
  | B | 폰트 지정을 현실에 맞춤 | 0 | 폴백을 전제로 스택을 다시 짬. 크기 유지, 개성 포기 |
  | C | 한글까지 인라인 | +1MB 이상 | 비현실적 — 채택하지 않음 |

- **권장** — **A**. 라틴이 뜨는 곳이 정확히 개성이 필요한 자리(워드마크·계기 라벨·형식번호)이고, 한글 본문은 시스템 폰트로도 충분히 읽힌다. 다만 폰트 파일 조달·라이선스 확인이 선행이다.
- **완료 기준** — 폰트가 없는 환경에서도 레이아웃이 깨지지 않을 것(`size-adjust`로 폴백 메트릭 보정).

#### P2-11. 결과 화면이 숫자 격자다

- **현재** — 2×2 수치 그리드. 한 판을 끝낸 감정을 받아주지 못한다.
- **목표** — 최고 기록 대비 위치, 이번 판의 최대 콤보 구간, 탑승자·기체 조합 표기를 시각화. 신기록일 때만 별도 연출.
- **손댈 곳** — `#scr-result`, `endRun()`.
- **완료 기준** — 스크롤 없이 한 화면에 들어갈 것(390px 기준).

#### P2-12. 스토리 화면에 화자의 시각 표현이 없다

- **현재** — 이름표 + 대사 텍스트. 관제/탑승자/기록이 색으로만 구분된다.
- **목표** — 화자별 표식(관제 = 계기 프레임, 탑승자 = 해당 식별 표식, 기록 = 표식 없음). 탑승자를 바꿨을 때 대사가 갈린다는 사실이 화면에서 바로 읽히게.
- **손댈 곳** — `showStory()`, `.line` 스타일.
- **완료 기준** — 탑승자 4명의 같은 구역 화면을 나란히 놨을 때 누가 말하는지 즉시 구분.

#### P2-13. 격납고에서 비교가 안 된다

- **현재** — 카드마다 절대 수치(공격 134%)만. 지금 장착한 것보다 나은지 판단하려면 탭을 오가야 한다.
- **목표** — 현재 장착 대비 증감(`+34%` / `-18%`)을 함께. 이미 있는 `.stat.up/.dn` 색 규칙을 그대로 쓴다.
- **손댈 곳** — `statChip()`에 기준값 인자 추가, `unitCard()`.
- **완료 기준** — 장착 중인 카드에는 증감을 표시하지 않을 것(자기 자신과 비교는 무의미).

#### P2-14. 도장 4종이 전부 단색이다

- **현재** — 기체 전체가 한 색. 기계화 도장이라기엔 단조롭다.
- **목표** — 도장을 **2톤 + 데칼**로. 주색/보조색을 나누고 패널 일부만 보조색, 형식번호 스텐실을 기체에 얹는다.
- **손댈 곳** — `SKINS`에 `col2` 추가, `drawFrame()`의 플레이트 분리(`DETAIL[id].accentPlates` 신설).
- **완료 기준** — 게임 중 1배 크기에서 2톤이 오히려 산만해지지 않을 것 — 안 되면 LOD로 큰 배율에서만.

#### P2-15. 사운드가 기계화 톤이 아니다

- **현재** — 사각파 blip 5종. 세계관과 어긋난다.
- **목표** — 금속성으로. 저역 임팩트 + 짧은 노이즈 버스트(장갑 타격), 발사는 공압식 짧은 처프, 보급 회수는 릴레이 클릭. WebAudio 노이즈 버퍼 + 밴드패스면 외부 파일 없이 된다.
- **손댈 곳** — `Snd`. 노이즈 버퍼는 1회 생성 후 재사용.
- **완료 기준** — 연사 시 소리가 뭉치지 않을 것(동시 발음 수 제한).

---

### P3 — 선택 (방향 결정 후)

#### P3-16. 탑승자 시각화 — 결정 필요

- **현재** — 얼굴 없이 계기판식 식별 표식(회피율만큼 열린 링 + 육각 + 번호).
- **선택지** — ⓐ 지금 방향 유지(세계관에 가장 맞고 추가 비용 0) / ⓑ 일러스트를 외부에서 받아 배선(파일 주면 규격 정리 후 적용) / ⓒ 얼굴 없는 파일럿 슈트 실루엣 + 바이저.
- **이 저장소에서 못 하는 것** — 래스터 일러스트 생성. 이미지 생성 수단이 없다. ⓑ는 외부 조달이 선행이다.

#### P3-17. BGM · P3-18. 파비콘/OG · P3-19. 무한 모드 시각 차별화

- BGM: 코드 생성 앰비언트(저역 드론 + 불규칙 릴레이)로 외부 파일 없이 가능. 반복 피로가 관건.
- 파비콘/OG: 현재 문서에 `<link rel="icon">`이 없다. 기체 실루엣을 인라인 SVG data URI로.
- 무한 모드: 「잔향」이 5구역과 같은 배경을 쓴다. 탈색 진행으로 차별화(P0-2에 포함).

### 추가 발견 (2026-07-28 코드 감사)

정합성 결함 4건은 이 감사에서 바로 고쳤다(5장 하단 기록). 아래 둘은 **설계 판단이 필요해 남겨둔다.**

#### A-1. 무한 모드 난이도 곡선이 선형이라 어느 지점에서 무너진다

- **현재** — `genWave()`의 `extra = max(0, stage - 3)`가 상한 없이 계속 더해진다. 실측:

  | 구역 | 총 적 수 | 한 웨이브 최대 | 적 HP 배율 | 보스 HP | 적 탄속 |
  |---|---|---|---|---|---|
  | 5 | 41 | 10 | ×2.5 | 1,460 | 260 |
  | 12 | 76 | 24 | ×5.2 | 3,280 | 344 |
  | 20 | 116 | **40** | ×8.2 | 5,360 | 440 |

  20구역의 돌입기 웨이브는 40기가 한꺼번에 달려든다. 무기는 5레벨이 상한이라 화력은 멈추는데
  적만 계속 늘어나므로, 실력이 아니라 **산술로 지는 지점**이 생긴다.
- **결정할 것** — 무한 모드를 ⓐ 명확한 종료점이 있는 구간으로 볼지, ⓑ 진짜 무한으로 둘지.
  ⓑ라면 동시 출현 수에 상한(예: 16)을 걸고 그 이후는 **수 대신 질**(패턴 난이도·HP·탄속)로 올려야 한다.
- **손댈 곳** — `genWave()`의 `extra` 상한, `hpScale()` 곡선, 적 탄속의 `G.stage` 계수.

#### A-2. 스토리 화면의 「진입」과 「건너뛰기」가 완전히 같은 동작이다

- **현재** — 두 핸들러가 같은 `storyThen` 콜백을 호출한다. 대사가 타이핑 연출 없이 한 번에 다 보이므로
  건너뛸 대상 자체가 없다. 버튼이 둘인데 결과가 같으면 사용자는 차이를 찾느라 멈칫한다.
- **선택지** — ⓐ 「건너뛰기」 삭제(가장 단순) / ⓑ 대사에 타이핑 연출을 넣고 「건너뛰기」가 그걸 마치게 /
  ⓒ 「건너뛰기」를 "이 구역 대사 다시 보지 않기"로 바꿔 `seenStory`에 기록.
- **권장** — ⓑ. 기계화 톤에 계기 출력이 한 줄씩 올라오는 연출이 잘 맞고, 그때 비로소 건너뛰기가 의미를 갖는다.

### 밸런스 기준선 (2026-07-29, 자동 조종 시뮬레이션)

`update()`를 직접 돌리는 자동 조종 하네스로 장비 레벨별 12판씩 측정했다.
위협 회피 + 최근접 적 조준 + 위험 시 폭탄을 쓰는 봇이라 **사람보다 잘 산다** —
난이도를 이 봇 기준으로 맞추면 안 되고, **경제·콤보 같은 수치 지표에만** 쓴다.

| 장비 LV | 도달 구역 | 코어/판 | 처치 | 최대 콤보 |
|---|---|---|---|---|
| 1 | 4 | 132 | 100 | 11 |
| 3 | 5 | 174 | 169 | 17 |
| 5 | 7 | 266 | 229 | 26 |

**기체별 성능** (LV3 장비 · 스트레이 · 각 10판, 무장 도입 후)

| 기체 | 도달 | 점수 | 처치 |
|---|---|---|---|
| TR-04 그레일 (제식 사격) | 4 | 44,267 | 161 |
| HV-09 보어 (집속 랜스) | 5 | 55,775 | 183 |
| SL-02 윔지 (광각 산탄) | 4 | 40,193 | 146 |
| XN-11 케이지 (측면 사출) | 4 | 37,262 | 137 |

> ⚠️ 이 표를 근거로 케이지를 상향하지 말 것. 봇은 200초 안에 거의 죽지 않아
> **생존 가치(육각 차폐)가 측정되지 않는다.** 보어가 앞서는 것도 봇이 표적을
> 정확히 추적하기 때문이며, 집속 랜스의 실제 약점(폭이 없다)은 사람이 칠 때 드러난다.
> 이 지표는 "쓸 수 없는 기체가 있는가"를 보는 용도로만 쓴다(137~183, 전부 사용 가능).

조정 전에는 코어가 49~75/판이고 전체 해금이 6,755여서 **약 113판**이 필요했다.
간단한 게임에 비해 과한 그라인드라 두 방향으로 손봤다.

- 강화 비용을 `40·lv^1.75` → `18·lv^1.6`로(슬롯당 905 → 345), 탑승자·기체·치장도 인하.
  이 게임의 핵심 콘텐츠(탑승자·기체)가 가장 늦게 열리면 안 된다는 판단.
- **구역 클리어 보너스**(`15 + 5·구역`)를 넣어 같은 구역 반복보다 깊이 들어가는 쪽이 이득이 되게.

결과: 전체 해금 4,075 · **약 23판**. 첫 강화가 20코어라 1판이면 몇 단계를 산다.

콤보도 4킬마다 +0.5로 바꿨다. 5킬 기준일 때 실측 콤보(11~26)가 ×2.0~×3.5에 머물러
상한 ×5~6이 닿지 않는 장식이었다. 지금은 같은 실력에서 ×2.5~×4.5, 40킬이면 상한.

> 하네스는 저장소에 없다(테스트 스크립트). 다시 재려면 `simRun(stage, eq, pilot, frame)`을
> 페이지에 주입해 `while(!G.over) { autopilot(); update(1/60); }` 형태로 돌린다.

---

## 3. 내가 정할 수 없는 것

작업 전에 답이 필요하다.

1. **탑승자 시각화 방향** (P3-16) — ⓐ/ⓑ/ⓒ. ⓑ면 일러스트 조달 계획이 먼저.
2. **폰트 인라인 여부** (P2-10) — 파일 크기 +40~80KB를 받아들일지. 폰트 라이선스 확인 주체.
3. **우선순위 확인** — 이 문서는 "보이는 격차 → 감각 → 완성도" 순으로 정렬했다. 다른 기준(예: 플레이 타임 늘리기)이 우선이면 순서가 바뀐다.

---

## 4. 작업 루프

이 저장소에서 디자인 작업은 **눈으로 확인하면서** 한다. 추측으로 넘어가지 않는다.

1. 헤드리스 Chromium으로 실제 렌더 → PNG
2. **다크/라이트 × 데스크톱(760px)/모바일(390px)** 네 조합 확인
3. 확대 렌더(3~5배)로 디테일 검증 — 1배에서 안 보이던 문제가 여기서 나온다
4. 콘솔 에러 0, 가로 스크롤 없음 확인
5. 성능에 영향 있는 변경이면 5장 기준선과 비교

기체·군체를 손댈 때는 **1배·2.9배·5배를 한 캔버스에 나란히** 그려 LOD가 모든 크기에서 성립하는지 본다.

---

## 5. 기준선 (2026-07-27, 커밋 `4853317`)

새 작업 후 이 수치를 다시 재서 비교한다.

**성능** — 최대 부하(5구역 보스 + 군체 17 + 적 탄 25 + 파티클 23)

| | 값 |
|---|---|
| 프레임 시간 중앙값 | 16.7 ms |
| p95 | 17.2 ms |
| 최대 | 20.6 ms |
| 파일 크기 | 101 KB |

> ⚠️ 16.7ms는 vsync 상한이라 **실제 그리기 비용이 아니다.** 여유가 얼마나 남았는지는 이 숫자로 알 수 없다.
> P0-1처럼 렌더 비용을 올리는 작업 전에는 `draw()`만 따로 계측해 실제 비용을 먼저 확보한다.

**대비비** (본문 4.5 / 큰 글씨 3.0 기준)

| 조합 | 다크 | 라이트 |
|---|---|---|
| `signal-t` on field | 7.26 | 5.47 |
| `drift-t` on field | 8.45 | 5.82 |
| `dim` on field | 6.07 | 4.93 |
| `moss` on field | 7.31 | **3.49** ← P0-3 |
| `fg` on field | 17.5 | 17.5 |

**터치 타겟** (390px 실측, 권장 44×44)

| 요소 | 크기 |
|---|---|
| `.tab` | 66×**34** |
| `.buy` | 55×**33** |
| `.iconbtn` | 69×**25** |

**2026-07-28 코드 감사에서 고친 정합성 결함 4건**

| 결함 | 재현 | 조치 |
|---|---|---|
| 런 종료 처리가 중복 실행돼 **코어가 그 횟수만큼 중복 적립** | 「출격 중단」 연타 (결과 화면이 뜨기 전 700ms 동안 버튼이 살아 있음) | `G.over` 플래그로 1회만 통과 |
| 사망 후 결과 화면 전 700ms 동안 HUD 조작 가능 | 그 사이 정지 → 중단으로 코어 2배 | 종료 시 즉시 `hud.inert = true` |
| 일시정지 중 HUD 버튼이 **키보드 포커스로 도달** | 오버레이는 마우스만 가린다. Tab → Enter로 정지 중 폭탄 소모 | `setScreen()`에서 `inert` 처리 + `useBomb()` 자체에 화면 상태 검사 |
| 손상된 저장 데이터로 **게임 전체가 죽고 새로고침해도 복구 불가** | `localStorage`에 `eq.weapon: 99` → `WEAPON[99].shots` 예외 | 로드 시 레벨·수치 clamp, 없는 탑승자·기체·치장 id는 기본값으로 |

`inert`는 입력 경로만 막는다. 불변식은 함수 안에서 다시 확인해야 한다 — 위 세 번째 항목의 교훈.

**미변경 확인**

- `drawBackdrop()`이 `G.stage`를 참조하지 않음 → 5구역 배경 동일 (P0-2)
- `REDUCED` 적용 지점 3곳뿐 — 파티클 수, 배기 파티클, 화면 흔들림. 히트스톱·연출류를 추가하면 함께 늘려야 한다
