/* 소리의 '악보' — 무엇이 어떤 파형으로 나는가.
   웹은 이걸 WebAudio 그래프로, 앱은 PCM 으로 렌더한다. 소리를 내는 방식은 달라도
   **숫자는 한 곳에만** 있어야 두 화면의 소리가 갈라지지 않는다.

   부품
     ["noise", 중심주파수, Q, 길이, 음량]   밴드패스를 지난 짧은 노이즈 = 금속성
     ["blip",  주파수, 길이, 파형, 음량]     단순 오실레이터 = 전자음
     ["sweep", 시작, 끝, 길이, 음량]         지수 주파수 램프 = 임팩트의 저역

   기계화 톤 — 공압식 발사, 금속 타격, 저역 임팩트, 릴레이 클릭.
*/

/* 전체 음량. 원래 값들이 -36~-19 dBFS 로 너무 작아 폰에서 거의 안 들렸다.
   비율은 그대로 두고 전체만 올린다 — 최악 동시 발음(8성부)이 0.28 → 0.69 로,
   클리핑(1.0)까지는 아직 여유가 있다. */
export const MASTER = 2.5;

export const SFX = {
  shoot: [["noise", 1900, 9, .045, .05], ["blip", 320, .03, "square", .012]],
  hit:   [["noise", 2600, 5, .05, .07]],
  boom:  [["noise", 700, 1.2, .22, .10], ["sweep", 240, 45, .26, .05]],
  big:   [["noise", 420, .8, .55, .14], ["sweep", 150, 28, .7, .08]],
  coin:  [["blip", 1180, .05, "triangle", .03], ["noise", 4200, 12, .03, .03]],
  hurt:  [["noise", 900, 2, .18, .12], ["sweep", 280, 80, .22, .07]],
  bomb:  [["noise", 300, .6, .5, .12], ["sweep", 80, 820, .35, .06]],
  ui:    [["noise", 3000, 14, .025, .05]],
};

/* 코어가 파라미터째 부르는 blip 들 — 앱은 이 목록을 미리 구워 둔다 */
export const BLIPS = [
  [300, .05, "sawtooth", .02],    // 직조체 사격
  [240, .06, "sawtooth", .022],   // 포탑 사격
  [520, .16, "triangle", .05],    // 육각 차폐
  [760, .07, "triangle", .035],   // 회피
  [1560, .09, "triangle", .04],   // 보스 격파
  [160, .09, "sawtooth", .03],
];

/* 동시 발음 상한 — 연사 시 소리가 뭉치지 않게 */
export const VOICE_CAP = 8;
