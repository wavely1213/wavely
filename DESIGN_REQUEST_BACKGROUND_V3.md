# 와벨리 — 동네레벨 **배경 아이템 v3** 제작 요청 (2026-07-27)

> v2 배경 9종을 **레이어 구조로 재설계**해 주세요. 사장님 피드백: **"지금 너무 선 구성이라 밋밋하다. 더 화려하게."**
> 구조·데이터 계약은 그대로. **비주얼만** 다시 만듭니다. 산출물 = `와벨리 동네레벨 v3.html` + `NEIGHBORHOOD_LEVEL.md §3-4` 갱신.

---

## 1. 지금 왜 밋밋한가 (v2 현행 분석)

v2 배경 9종이 **전부 같은 공식 1개**로 만들어져 있습니다:
```css
.pat { background: var(--patc);      /* 단색 1개 */
       opacity: var(--patop);        /* .16(light) / .24(dark) */
       mask-image: var(--pat);       /* 선·점 SVG 타일 */
       mask-size: var(--patsz); mask-repeat: repeat; }
```
| key | 이름 | 해금 | 타일 | 틴트(라이트/다크) |
|---|---|---|---|---|
| `plain` | 단색 | 기본 | — | `--patop:0` |
| `wave` | 물결 | Lv 1 | 128×44 | `--primary` |
| `dots` | 물방울 | Lv 5 | 26×26 | #38BDF8 |
| `ripple` | 파문 | Lv 15 | 72×72 | #0EA5E9 |
| `mountain` | 봉의산 능선 | Lv 25 | 180×64 | #3F8F6B / #5FBE92 |
| `lake` | 의암호 윤슬 | Lv 40 | 104×44 | #2C7DA0 / #5AB0D4 |
| `sakura` | 벚꽃 흩날림 | 봄 시즌 | 78×78 | #DB6E97 / #F5A3C0 |
| `firework` | 축제 불꽃 | 마임축제 인증 | 88×88 | #F26D1F / #FF9A5A | 애니 1종 |
| `founder` | 창단 물결 | 출시 첫 달 | 56×56 | #C08D12 / #F2DA85 |

**문제**: 단색 + 낮은 투명도(.16~.24) + stroke 위주 타일 → **9종이 무늬만 다를 뿐 인상이 거의 같습니다.** 착용해도 "내가 뭘 꼈는지" 자랑이 안 되고, 수집욕이 안 생깁니다. 애니는 `firework` 1종뿐, 그마저도 "투명도 호흡 + 7px 드리프트"입니다.

---

## 2. 요청 — 레이어 구조 + 등급 비례 화려도

전부 화려하게가 아니라 **위로 갈수록 확 화려하게**. 하위가 절제돼야 상위가 빛납니다.

### 2-1. 3레이어 구조로 재설계 ★핵심
| 레이어 | 역할 | 비고 |
|---|---|---|
| ① 베이스 워시 | 단색 → **그라디언트**(선형/방사/메시) 2~3스톱 | 지금 없는 층 |
| ② 패턴 | 현 마스크 타일 유지(형태 식별) — **stroke 위주 → 면(fill)·볼륨 혼합** | 개선 |
| ③ 하이라이트 | 글로우·광택·비네트·스파클 중 1 | 지금 없는 층 |

### 2-2. 등급 비례 스케일링
| 티어 | 레이어 | 모션 |
|---|---|---|
| `plain`·`wave` (Lv1~) | 1 (현행 수준) | 없음 |
| `dots`·`ripple` (Lv5·15) | 2 | 없음 |
| `mountain`·`lake` (Lv25·40) | 3 | 미세(선택) |
| `sakura`·`firework`·`founder` (시즌·희소) | 3 + 모션 | 있음 |

### 2-3. 시즌·희소 3종은 확실히 튀게
- **sakura**: 분홍 그라디언트 + 꽃잎 **실제 형상(면)** + 낙하 모션
- **firework**: 발광(글로우) 중심, 어두운 배경일수록 강하게
- **founder**: 금색 광택 + 스파클 (재획득 불가 = 최고 희소)

### 2-4. 투명도 상한 상향
현행 `.16/.24` → 상위 등급은 더 진하게. **단 §3 가독성 가드레일 준수.**

---

## 3. 가드레일 (반드시 지켜주세요)

1. **가독성** — 히어로·카드 위 텍스트 대비 **WCAG AA** 유지. 배경이 글자를 이기면 커뮤니티가 안 읽힙니다.
2. **적용 범위 불변** — 배경은 **프로필 히어로 · 도감 장착 카드 · 리그 헤더**에만. **피드 행·댓글엔 절대 금지**(현행 §밀도보호 그대로).
3. **`prefers-reduced-motion: reduce`** — 모든 모션 정지 경로 필수.
4. **앱(RN) 성능** — `react-native-svg <Pattern>` + 다중 레이어는 비쌉니다.
   - **앱은 정적 2레이어까지**, 3레이어·모션은 웹 우선
   - 앱용 **대체 표현을 함께** 주세요(예: 그라디언트 1장 + 정적 패턴, 모션은 생략)
   - ⚠️ `expo-linear-gradient` **미설치** → 그라디언트는 `react-native-svg`의 `<LinearGradient>/<RadialGradient>`로 구현 가능한 형태로 주세요(신규 네이티브 의존성 추가 시 dev-client 재빌드 필요)
5. **cosmetic-only 불변** — 화려해져도 검색순위·광고노출·매장추천에 영향 0. (이건 시스템 최상위 원칙)
6. **웹은 conic/mask 자유**, 단 다크모드 자동 대응(색을 SVG 안에 넣지 말고 `--patc` 틴트 방식 유지 권장)

---

## 4. 데이터 계약 (변경 없음 — 그대로 두세요)
```ts
unlocks[].kind: 'badge' | 'title' | 'border' | 'background'
equip_background(key)          // key = plain|wave|dots|ripple|mountain|lake|sakura|firework|founder
get_level_card(user) → { ..., equipped_background }
```
- **키 9개 유지**(이름 바꾸지 마세요 — DB·CSS 클래스·앱 매핑이 이 키를 씁니다)
- 새 배경을 추가하고 싶으면 **키를 새로 제안**해 주세요(기존 키 재사용 금지)

---

## 5. 산출물
1. **`와벨리 동네레벨 v3.html`**
   - 배경 9종 **레이어 구조로 재설계**
   - **등급 스케일링 비교 뷰**(plain→founder까지 한 화면에 나란히)
   - 라이트/다크 양쪽, 히어로·도감카드·리그헤더 3개 적용 지점 모두 예시
2. **각 배경의 정확한 값** — 그라디언트 스톱·투명도·마스크 크기·모션 주기
   - **웹(CSS)** 과 **앱(RN 대체안)** 을 각각 표기
3. **`NEIGHBORHOOD_LEVEL.md §3-4` 갱신**

---

## 6. 개발 현황 (참고 — 지금 재설계해도 버리는 작업 0)
- **웹**: 링 conic 9종·레벨카드·도감 모달 이식 완료. **배경은 아직 미이식** → 지금 바꿔도 손실 없음
- **앱**: 레벨 카드만(SVG 선형 3스톱 링). 도감 화면·배경 미구현
- **DB**: `_DRAFT_level_system.sql` 미배포 → 레벨 UI 전체가 **dormant**(안 보임) 상태
- 즉 **비주얼 확정 후 한 번에 붙이는 게 가장 효율적**입니다

## 7. 열린 항목 (같이 정해주시면 좋음)
1. `firework` 해금 조건 "춘천 마임축제 참여 인증" — 인증 **주체·방법** 미정
2. `sponsor_*` 매장 스폰서 배경도 만들지 (매장별 색 주입 방식이면 팔레트 규칙 필요)
3. 배경 미리보기를 도감에서 **실제 히어로 크기**로 볼 수 있게 할지(작은 스와치만으로는 화려도 판단이 어려움)
