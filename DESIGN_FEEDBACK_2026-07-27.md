# 와벨리 — 디자인 보완 요청 (개발 반영분 기준) 2026-07-27

> 이식·QA 중 **코드에서 먼저 고친 것들**입니다. 디자인 원본과 어긋난 채로 두면 다음 핸드오프 때 다시 되돌아가므로, 아래를 반영해 **디자인 쪽 정본을 갱신**해 주세요.
> 구조·데이터 계약은 그대로입니다. **비주얼/상태 정의만** 보완 요청합니다.

---

## A. 반드시 반영 (개발에 이미 적용됨 — 디자인이 따라와야 함)

### A-1. 브랜드 색 = 스카이블루로 통일 (그레이프 폐기)
- 4개 서피스(앱·소비자웹·관리자웹·랜딩) 전부 스카이로 교체 완료.
- 매핑: `#7A2BC4→#0EA5E9` · `#641FA6→#0E7490` · `#F3EAFB→#EAF7FB` · `#E6D3F6→#C6E9F7` · (dark) `#9D5FE6→#38BDF8` · `#C2A2F2→#7DD3FC` · `#1E1335→#0C2A3F` · `#2C1E46→#123A52`
- **다크 프로모 카드**(사이드바 "사장님이신가요?") : `#221C30`(보랏빛) → **`#14212B`**(스카이 계열 다크)
- rgba 그림자: `rgba(122,43,196,…)` → `rgba(14,165,233,…)`
- ⚠️ **예외(바꾸지 말 것)**: 알바 업종색 `사무·기타 #7A2BC4` — 이건 브랜드색이 아니라 `ALBA_JOB_TAB.md §1`의 업종 구분색 스펙.
- 요청: `wavely.tokens.ts / .json`을 스카이 기준으로 갱신해 주세요(현재 파일이 아직 그레이프라 혼선의 원인이었음).

### A-2. 사이드바 — 프로필 진입점 3중 중복 → 2개로 정리
- 기존: ①메뉴 "마이페이지" ②**좌하단 유저 버튼**(아바타+닉+역할) ③우상단 아바타 → 셋 다 같은 화면.
- **②(좌하단) 삭제**했습니다. 디자인에서도 제거해 주세요.
- 현재 사이드바 순서: `로고 → 지역선택 → 메뉴5 → 글쓰기 → (여백) → 사장님카드 → 법적푸터`

### A-3. 상단바 아바타 = 상태별 동작 정의 필요
- 버그였음: 로그인 상태에서도 **항상 로그인 모달**이 떠서 "무한 로그인창"이 됨.
- 확정 동작: **로그인 시 → 마이페이지 / 비로그인 시 → 로그인 모달**.
- 요청: 아바타의 **로그인/비로그인 두 상태**를 디자인에 명시(툴팁·이니셜·게스트 표현 포함).

---

## B. 디자인 결정이 필요한 것 (개발은 임시로 처리함)

### B-1. 사이드바 세로 오버플로 규칙 ★
- 문제: 사이드바 요소가 많아 **창 높이가 짧으면**(노트북·창 축소) 넘칩니다. 이전엔 `글쓰기` 버튼이 **눌려 납작해지고**(46px→축소) 아래 카드에 붙었고, 하단(사장님카드·약관·계정)이 **잘린 채 스크롤도 불가**했습니다.
- 개발 임시조치: 글쓰기 버튼 `flex:none`(46px 고정), 여백 최소 16px, 사이드바 `overflow-y:auto`.
- 실측(1280×480 기준): 좌하단 제거 후에도 **약 213px 넘침**.
- **요청**: 짧은 화면에서의 규칙을 디자인해 주세요. 택1 또는 조합
  - (a) 사이드바 전체 스크롤 허용 (현재 임시안)
  - (b) 사장님 카드를 **접힘/축약형**으로 (아이콘+한 줄)
  - (c) 법적 푸터를 **더보기 안으로** 접기
  - (d) 특정 높이 이하에서 메뉴 아이콘만 남기는 **컴팩트 모드**
- 최소 보장값(개발 기준): 글쓰기 버튼 높이 **46px 고정**, 버튼↔카드 간격 **≥16px**.

### B-2. 축제(행사) 탭 — 축제 전용 오렌지 테마
- 신설했고 **이 탭에서만** 축제 브랜드색을 씁니다: `#F26D1F`(deep `#C7500F`, soft `#FDECE0`), 닭갈비 `#E8461E` / 막국수 `#2A9D8F`. 나머지 앱은 스카이 유지.
- 현재 구성: 히어로(축제명·기간·D-day·장소) → 퀵버튼4(전화·길찾기·인스타·공식) → 축제 한눈에 → 오시는 길(지도) → **프로그램/타임테이블** → 관람안내 타일6 → 참여업체 → 푸터.
- **요청: 아래 "상태"들을 디자인해 주세요** (실데이터가 아직 미공개라 상태 디자인이 중요)
  1. **타임테이블 빈 상태** — 날짜칩(10.14~18)은 있는데 일정 미공개 → "공개 예정" 표현
  2. **타임슬롯 → 상세 시트** — 유형칩(공연/먹거리/체험)·시간·제목·장소·설명. 현재 바텀시트
  3. **지도 폴백** — 지도 API 미인증/실패 시 "주소 + 카카오 길찾기" 행
  4. **참여업체 미정 상태** — 업체 선정 전 안내 + 모집공고 링크
  5. (선택) 축제 진입 배너 — 커뮤니티/우리동네 상단, 현재 오렌지 그라데이션 바

### B-3. 동네레벨 — 앱(RN) 제약 + 미완 화면
- 웹은 핸드오프 링 CSS를 **원본 그대로** 이식했습니다(메탈릭 conic 9종 정상).
- **앱(RN)은 `conic-gradient` 불가** → SVG 선형 그라디언트(3스톱)로 근사했습니다.
  - **요청**: 앱용 링을 **선형 3스톱 기준으로 재정의**해 주세요(등급별 시작/중간/끝 색). 지금은 제가 conic 스톱에서 임의 발췌한 상태라 디자인 정본이 필요합니다.
  - 앱에서 회전 애니메이션(다이아 7s)·파티클 이펙트는 미적용 — **앱에서 어디까지 살릴지** 지정 필요.
- **미완 화면**: 앱에는 레벨 카드만 있고 **아이템 도감 화면이 없습니다**(웹은 모달로 있음). 앱용 도감 화면 필요.
- 참고: DB(RPC) 미배포라 현재 웹·앱 모두 **자동 dormant**(안 보임). 배포 시 자동 노출.

### B-4. 알바 탭 — 앱이 핸드오프 대비 축소 구현
- 웹은 스펙대로 완성(세그먼트·정렬3·카테고리 다중+드래그 페인팅·동네 시트·러브콜).
- **앱은 일부만**: 구인/구직/전체 · 정렬(가까운순·시급순·최신순) · 카테고리 다중선택 · 동/검색 · 카드(광고링·마감·익명). 
- 미적용: **드래그 페인팅**(모바일은 가로스크롤과 충돌), **업종 로고+업종색 아바타**, 결과 요약줄의 위치 버튼 UI.
- **요청**: 모바일에서 카테고리 칩의 **다중선택 인터랙션 대안**(드래그 대신)과, 앱 카드에 업종색을 어디까지 쓸지 지정.

---

## C. 공통 요청 형식
- 화면별 HTML/컴포넌트, **light·dark 둘 다**, **모바일 우선**(≤760px 단일 컬럼).
- 자체완결(외부 폰트·CDN 없이, 아이콘 인라인 SVG), 색은 CSS 변수.
- 색·간격·라운드는 **갱신된 `wavely.tokens`** 기준. 신규 hex는 (축제 오렌지·등급 링 재질처럼) 목적이 분명할 때만.

## D. 참고 (현재 라이브)
- 소비자웹: `wavely-web-three.vercel.app` (도메인 mulgyeol.kr 연결 대기)
- 위 A 항목은 **이미 실서버 반영됨**. B는 임시 처리 상태 → 디자인 확정되면 교체합니다.
