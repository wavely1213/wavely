# ⚠️ 와벨리 디자인 충실도 규칙 — repo의 `CLAUDE.md`에 그대로 붙여넣으세요

> Claude Code가 매번 "비슷하지만 변형"하는 걸 막는 핵심 파일입니다.
> 아래 블록을 `workmate/CLAUDE.md`(또는 루트 CLAUDE.md)에 추가하면 모든 세션이 이 규칙을 따릅니다.

---

```md
## 와벨리 디자인 규칙 (필수 준수 — 변형 금지)

이 저장소의 UI를 만들거나 고칠 때는 **재해석하지 말고 그대로 재현**한다.

### 단일 진실 소스
- 색·라운드·그림자·간격·폰트·배지색은 **`wavely.tokens.ts`(= src/constants/theme.ts)** 값만 사용한다.
  새 색/간격을 발명하지 않는다. 하드코딩 hex 금지 — 항상 토큰을 import 한다.
- 디자인 레퍼런스는 핸드오프의 프로토타입 HTML이다. 그것이 **픽셀 스펙**이다.

### 하지 말 것 (변형 금지 목록)
- 색·폰트·라운드·간격·그림자 임의 변경/대체 (예: Inter·Roboto로 폰트 교체 금지 — Pretendard 고정)
- 레이아웃 구조·네비게이션·화면 순서 재설계
- 컴포넌트/라벨/문구 이름 바꾸기, 섹션 추가·삭제, "개선" 명목의 변경
- 그라데이션·이모지·아이콘을 임의 추가 (스펙에 있는 것만)
- 광고 3종(배너/인피드/플레이스)·역할 4종·게시판(자유/홍보/사장님/직장인) 구성 변경

### 반드시 할 것
- 플랫 기조 유지 — 그림자 대신 얇은 구분선/테두리로 면을 구분 (라운드 16~18px 유지)
- 모르거나 빠진 값이 있으면 **추측하지 말고 질문**한다
- 한 번에 한 화면씩, 핸드오프의 해당 파일을 열어 1:1로 맞춘다
- 끝나면 프로토타입 스크린샷과 나란히 비교해 차이를 보고한다

### 아키텍처 (ARCHITECTURE.md 참조)
- 앱·소비자 웹·관리자 웹은 **같은 Supabase 프로젝트**를 공유한다. 별도 DB 만들지 않는다.
- 마케팅 사이트만 백엔드 없는 별도 정적 배포다.
```

---

## 세션 시작 시 붙여넣을 프롬프트 (권장)
> 아래를 매 작업 첫 메시지에 붙이면 드리프트가 크게 줄어듭니다.

```
첨부한 와벨리 핸드오프대로 <화면명>을 구현해줘.
- wavely.tokens.ts 값만 사용(하드코딩 hex 금지, 폰트 Pretendard 고정)
- 프로토타입 HTML을 픽셀 스펙으로 삼고, 레이아웃·문구·구성·색을 그대로 재현
- 재설계·이름변경·섹션추가·"개선" 금지. 빠진 값은 만들지 말고 나에게 물어봐
- 기존 RN/NativeWind 패턴·기존 Supabase 스키마 그대로 사용
완료 후 프로토타입과 다른 점만 목록으로 알려줘.
```
