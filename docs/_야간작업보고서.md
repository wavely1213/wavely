# 🌙 야간 자율 작업 보고서

> 작업 시작: 2026-06-17 새벽 (~02시) · 종료: 아침 (8시 경)
> 형이 자리 비운 동안 자율 진행. **push 안 함(형 몫), 커밋만.**

## ☀️ 아침 결정 반영 (형 D1~D5 회신 후)
- **D1 키워드 상한 제거** ✅ — 5→30(사실상 무제한, 수집서버 보호). 클라+`10_keyword_limit.sql`. (완전무제한 원하면 10번 파일 주석 참고)
- **D2 차트 추가** ✅ — 일자별 추이(방문리뷰·블로그·저장·N2·N3) View 막대차트. 분석 2회+ 쌓이면 표시. (SVG 라이브러리 없이 구현)
- **D3 보안SQL 적용** ⏳ — **토큰 필요**(이전 토큰 폐기됨). 아래 "지금 형이 할 것" 참고. 09+10 같이 적용.
- **D4 reviews RPC 교체** ✅ — store/place 상세의 승인을 `admin_set_review_verified` RPC로. (09 적용돼야 동작 — 그래서 D3와 함께)
- **D5 키워드 자동탐색** ✅(스펙) — 수집기(260616) 작업이라 직접구현 X. `docs/플레이스랭크-수집기워커.md` 3.5절에 구현 스펙·데이터흐름 정리. 와벨리는 자동발견 키워드 자동표시(코드수정 불필요).

### ▶ 지금 형이 할 것 (D3)
1. 새 Supabase access token 발급(https://supabase.com/dashboard/account/tokens) 후 채팅에 붙여넣기 → 내가 09+10 적용. **또는**
2. SQL Editor에 `supabase/09_security_hardening.sql` → `supabase/10_keyword_limit.sql` 순서로 붙여넣고 RUN.
→ 적용되면 D4(reviews 승인)도 정상 동작. 끝나면 알려줘.

---

## ⚡ 한눈 요약 (먼저 이것만 봐도 됨)
1. **🔴 제일 먼저 할 것**: `supabase/09_security_hardening.sql` 을 Supabase SQL Editor에 붙여넣고 RUN. (별점 자가조작·신고자 신원노출·분석요청 폭주 등을 막음. 멱등이라 안전.) → 적용 후 알려주면 D4(클라 2줄) 같이 커밋.
2. **밤새 한 일**: 플레이스 분석 기능을 수집기 실제 구조에 맞게 정합 + adlog식 종합분석(노출점수·N2/N3·급등급락·순위분포) 이식 + 전방위 보안/악용 하드닝. 커밋 26개(이번 세션 누적, push는 형이).
3. **형 결정 필요 5건**: 아래 🔴표 (D1~D5). 가장 중요한 건 D3(보안SQL 적용)과 D5(키워드 자동탐색 우선순위).
4. **앱은 정상**: 모든 변경 프리뷰에서 렌더·콘솔에러0 검증. 데이터가 실제로 차려면 수집 서버 워커 가동 + 09 SQL 적용 필요.

---

## 🔴 형 결정/허가 필요 (아침에 한 번에) — 위가 우선순위 높음
> 자율로 못 한(또는 안 한) 것, 보안 트레이드오프, 외부행동, 비용/유료, 제품방향 결정.

| # | 항목 | 왜 형이 필요한가 | 내 추천 |
|---|---|---|---|
| D1 | **키워드 자동탐색 vs 수동등록** — 형은 adlog식 "키워드 안 정해도 자동"을 원했는데, **수집기(wavely_sync.py)는 등록 키워드(place_rank_keywords)만 분석, 자동탐색 미지원**. 내가 와벨리 UI를 자동탐색으로 바꿨다가 이 사실 발견 → 그대로 두면 분석 100% 실패("등록 키워드 없음"). | 수집기 능력과 UI 계약이 안 맞음. 자동탐색은 수집기(260616) 작업 필요. | **일단 수동등록 UI 복구해서 동작하게 함(아래 자율반영).** 자동탐색은 형이 수집기쪽 우선순위 정하면 그때. 키워드 상한은 현재 5개(07 SQL 트리거) — 늘릴지 결정. |
| D2 | **N지수 표시 범위** — 수집기는 N1=null(공식없음), N2/N3=등록키워드 평균만 산출. adlog 스크린샷의 일자별 추이 차트/급등급락/순위분포는 와벨리에 내가 추가 가능(client 계산). | adlog 수준까지 맞출지 범위 결정 | 자율로 급등·급락·순위분포·노출점수까지 추가(아래). 차트는 라이브러리 없어 보류(원하면 추가). |
| D3 | **🔴 보안 SQL `09_security_hardening.sql` 적용 필요** — 별점 자가조작·신고자 신원노출·분석요청 DoS 등 막는 통합본. 토큰 폐기하셨으면 SQL Editor에 붙여넣고 RUN. 새 토큰 주시면 내가 적용. (A)점검쿼리로 trust컬럼 UPDATE 행 사라졌는지 확인. | DB 변경이라 형 적용/토큰 필요 | **아침에 바로 적용 권장.** 이전 세션서 일부 이미 적용됐어도 멱등이라 안전. |
| D4 | **reviews.verified 클라 교체** — 09 적용 후 `store/[id].tsx`·`place/[id].tsx`의 `reviews.update({verified})` → `admin_set_review_verified` RPC로 바꿔야 관리자 검수 동작(09 적용 전엔 직접 update가 동작). 2줄 변경. | 09 SQL과 짝 | 09 적용 타이밍 정해지면 내가 같이 커밋. |
| D5 | **키워드 자동탐색 우선순위** — 현재 등록키워드 최대 5개. adlog식 자동탐색(20개+)은 수집기(260616)에 자동탐색 구현 필요. 우선순위? | 제품방향+수집기 작업 | 사장님 효용 큼. 수집기쪽 다음 큰 작업으로 추천. |

---

## ✅ 자율로 진행·반영한 것 (커밋됨)
> 명백히 안전·가치있어 바로 반영한 것. 시간순.

| 시각 | 분류 | 내용 | 커밋 |
|---|---|---|---|
| 02시 | 분석 | 수집기 코드 정독(nscore.py·wavely_sync.py 등) → N지수 진짜 원리·계약 파악 | - |
| 02시 | **버그수정(중대)** | 와벨리 자동탐색 UI ↔ 수집기 등록키워드 계약 불일치로 분석 100% 실패 예정이던 것 → **키워드 등록 UI 복구**(place_rank_keywords 쓰기·삭제 end-to-end 실DB 검증) | (Phase1 커밋) |
| 02시 | 수정 | 잘못된 종합점수(N1·N2·N3 합성, 스케일 불일치) 제거 → **노출 점수(0~100, DCG)** client 계산으로 교체 | 〃 |
| 02시 | 기능추가 | adlog식 종합분석 이식: **급등/급락 키워드, 순위분포 막대, 10위권밖 카운트** (모두 client 계산) | 〃 |
| 02시 | 문서 | N지수 계산식 doc을 수집기 실제 회귀공식 기준으로 정정(자작식 폐기 명시) | 〃 |
| 03시 | 보안감사 | 병렬 3에이전트(보안·악용·기능) 전수감사 → 발견사항 분류 | - |
| 03시 | **보안SQL** | `supabase/09_security_hardening.sql` 작성(멱등) — 가입메타 신뢰필드 무시, profiles/stores 트리장컬럼 UPDATE 차단+WITH CHECK, **reviews.verified 자가인증 차단(별점조작)**, **reports 관리자전용(신고자 보호)**, **분석요청 매장당 1건 유니크(수집기 DoS 차단)**, requested_by 위조차단, likes/scraps 유니크, 텍스트길이 CHECK. **형 적용 필요(아래 컨펌)** | (커밋) |
| 03시 | 악용방지 | 전 입력칸 **maxLength** 추가(글·댓글·채팅·닉네임·마켓·구인·리뷰·신고 — 렌더폭탄/저장DoS 방지) | 〃 |
| 03시 | UX/안전 | store-new 매장삭제 2단계 확인 + 네이버ID 숫자검증(place-rank와 동일) / place-rank 키워드 2글자↓·삭제에러·중복요청 친절안내 | 〃 |

---

## 🔎 점검 로그 (검토했지만 변경 안 함 / 관찰)
- **시크릿 안전 확인**: `.env`는 git 비추적(`.env.example`만 추적). service_role 키·시크릿은 코드/번들에 없음(엣지함수·수집기 env에만). ✅ 안 건드림.
- **이미 막힌 것(이전 세션)**: profiles/stores 트러스트컬럼(is_admin·biz_verified·is_ad 등) UPDATE 차단은 메모리상 이전에 적용됨 → 09 SQL에서 **재확인(멱등)**만. live DB에서 09의 (A)점검쿼리로 최종 확인 권장.
- **RLS는 DB가 진실원천**: 커밋된 SQL은 부분 마이그레이션이라 reviews/reports/likes 등 일부 테이블 정의가 repo에 없음. 09는 `테이블 있으면만` 방어적으로 작성. **권장: live `pg_policies`+grants 덤프를 한 번 repo에 박아두면 RLS를 버전관리로 리뷰 가능**(D 항목 아님, 권장).
- **결제/지갑**: 금액은 엣지함수(verify-payment 등) 서버검증 경유 — 클라가 잔액 직접 못 씀. 무한캐쉬는 is_admin 의도된 것. 안 건드림.
- **수집기 코드(260616)**: 단일 진실원천이라 **읽기만** 함(편집 안 함). N공식·워커 구조 참고만.

---

## 📌 다음(아침 이후) 권장 작업 — 안전하지만 시간상 못 한 것 / 추가로 할 것
- **client 자잘한 마감**(안전, 내가 이어서 가능): 등록했지만 아직 수집 안 된 키워드를 "분석 대기중"으로 목록에 표시 / 리뷰·매장등록 성공 토스트 / explore·notifications·keywords·store상세 pull-to-refresh / 마켓·구인 상세 공유버튼 / 이미지 업로드 실패 알림.
- **D4**: 09 적용 후 reviews.verified 클라 → RPC 교체(2줄).
- **수집기(260616)**: ①스케줄 백그라운드 수집(6시간마다)으로 "열면 즉시" 실현, ②키워드 자동탐색(D5), ③N1 공식(키워드 고정점수 — adlog 샘플 모으면 근사).
- **출시 잔여(기존)**: company.ts 사업장주소·약관시행일, PortOne 라이브, 도메인/앱스토어 — `docs/_미뤄둔질문.md` 참고.

---
_이 보고서는 아침 핸드오프용. 형이 D1~D5만 정해주면 바로 이어서 진행._
