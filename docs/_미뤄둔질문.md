# ⏸️ 미뤄둔 질문·결정 (형 돌아오면 한 번에)

> 자율 작업 중 허가·결정이 필요해 미뤄둔 것들. 2시간 뒤 같이 정리.

## 결정 완료
1. ✅ **비로그인 마켓/구인구직 접근 정책** — **결정: 목록은 공개, 상세는 로그인 게이트** (당근식 유입 + 보호 절충)
   - DB: `market_items` 본문(body), `jobs` 본문(body)·연락처(contact)를 anon 권한에서 제외(column-level GRANT). 비로그인 직접 호출 시 401 검증 완료.
   - 앱: 목록(`market.tsx`/`jobs.tsx`)은 비로그인도 열람. 상세(`market/[id].tsx`/`jobs/[id].tsx`)는 비로그인 시 본문·판매자/작성자·연락처·채팅 숨기고 🔒 로그인 게이트 노출. 로그인 시 전체 노출.
   - 검증: 두 상세 라우트 번들 정상 컴파일, 콘솔 에러 0.

## 📈 플레이스 분석(사장님) — 활성화에 필요한 것
- 와벨리 쪽 완성: `src/app/place-rank.tsx` — 종합분석 요약(N1~3 지수·순위·저장·리뷰), **온디맨드 '🔍 지금 종합분석' 버튼**(요청 큐+폴링), 키워드 관리, '다른 매장 분석=개발중'. 메인 빠른메뉴+내정보 진입(모두 노출, 비인증은 🔒게이트). 매장 등록/수정에 place_id 필드(기본 저장).
- **활성화 SQL 적용 필요(순서)**: `supabase/05`(있으면)·`06`·`07_place_rank.sql`(키워드·순위 테이블)·`08_place_analysis.sql`(온디맨드 큐+종합분석 결과). SQL Editor 붙여넣기 or 토큰 주면 내가 적용.
- **🔴 수집기 워커 필요(미작업 — 이게 있어야 버튼이 실제 동작)**: 집 PC 수집기가
  ① `daily.py` 끝에 Supabase로 일일 순위 push(매일), ②`place_analysis_requests`를 폴링해서 pending 요청 처리(온디맨드: 수집→N1~3 계산→`place_rankings`+`place_analysis` 적재→done). service_role 키 사용. 명세는 `07`/`08` SQL 하단 + `docs/플레이스랭크-연동검토.md`. **수집기는 다른 대화/260616 폴더에서 작업 중이라 여기서 직접 안 건드림(단일 진실원천 보호)** — 그쪽에서 위 워커 구현 필요.
- 적용+워커 후 흐름: 사장님이 place_id 연결+키워드 등록 → [지금 종합분석] 누르면 집 PC 수집기가 수집·분석 → 결과 표시. (수집기 꺼져 있으면 2분 후 "응답 없음" 안내)

## 🔴 형 입력/결정 필요 (1.0 감사에서 발견)
2. **[출시 블로커] 회사·법적 정보 입력** — `src/constants/company.ts`의 `address`(사업장 주소)와 `effectiveDate`(약관 시행일)가 아직 플레이스홀더(`(사업장 주소)`/`(시행일)`). `companyInfoReady=false`라 약관/개인정보 화면에 "정식 출시 전 입력"으로 노출됨. **전자상거래법상 판매자 주소 + 약관 시행일 필수** → 실제 사업자 주소와 시행일(예: 2026-06-17)을 알려주면 바로 채움. (사업자등록 전이면 개인 운영자 정보로라도)
3. **[서버 반영 필요] `supabase/05_store_insert_guard.sql` 적용** — 매장 생성 시 `biz_verified` 임의 설정을 서버에서 막는 트리거. 클라이언트 구멍은 이미 막았고(이건 REST 직접호출 방어). Supabase SQL Editor에 붙여넣거나, Management API 토큰 주면 내가 적용. (지금 이 환경엔 토큰 없음)
4. **[유료셋업 때] PortOne 라이브 전환 + "테스트 모드" 문구 제거** — `src/app/ad.tsx`에 "현재 테스트 모드" 실제 노출 문구 있음. 라이브 전환 시 함께 제거.

## ⚪ 서버(RLS) 확인 권장 (코드는 RLS 신뢰 중 — 직접호출 방어 점검)
- `reviews` 의 `verified` UPDATE / DELETE 가 관리자·소유자로 제한되는지 (별점 신뢰 조작 방지). 코드는 `.update({verified})`를 RLS에 의존.
- `reports` SELECT/UPDATE 가 관리자 전용인지 (클라 가드는 추가했지만 서버도 필요).
- `market_items.body`·`jobs.body`·`jobs.contact` 가 anon GRANT에서 빠져 있는지 (목록공개/상세게이트 정책의 서버 근거 — 앞서 401 확인했으나 출시 전 재확인 권장).

---

## ⚪ 의도적으로 안 한 것 (낮은 우선순위 — 원하면 진행)
- danger/warning 테마 토큰화(`#E5484D`/`#FF9F40` ~78곳) — 다크/라이트 일관성 폴리시, 시각적 영향 작아 보류.
- 폼 KeyboardAvoidingView(네이티브 전용 — 웹 프리뷰로 검증 불가).
- 피드 목록 당겨서 새로고침(pull-to-refresh).

---

## ✅ 자율 작업 진행 로그 (참고)
- 내활동에 '내 판매·내 공고' 탭 추가
- 내정보에 '동네 생활'(중고거래·구인구직·인기글·키워드) 섹션 추가
- **버그 2개 자율 발견·수정**:
  1. my.tsx else-if 체인 문법오류 → 번들 전체 깨졌던 것 수정 (커밋 amend)
  2. market_items/jobs의 seller·author FK가 auth.users 참조라 profiles 임베드(상세화면) 실패 → **profiles 참조로 변경**(DB, 라이브 반영). 상세화면 복구.
- 신규 화면 전수 검증: 마켓(목록·등록·상세), 구인구직(목록·등록·상세), 인기글, 키워드, 내활동, 빠른메뉴 — 실데이터로 정상.

### 추가 진행(자율 2~3차)
- 마켓·구인구직 채팅에 물건/공고 첫 메시지 자동(중복방지) `f03281f`
- 신고화면 market/job 라벨 `6dce454`
- 알림화면 '키워드 알림 설정' 진입 `d4ec191`
- 구인구직 목록 검색 추가 `b1f128a`
- 엣지케이스 QA 통과(사진0장·나눔·거래완료) — 버그 없음
- **남은 임팩트 큰 작업은 위 '비로그인 접근 정책' 결정 대기 중.**

### 자율 라이트 패스(2026-06-17)
- dev server(8081) 생존 확인. 홈 아이콘버튼 접근성 라벨(알림·검색지우기) 추가·검증·커밋(`d966e59`).
- 빈상태/에러문구 전수 점검 → 이미 일관됨(수정 안 함). 더 손댈 안전한 항목 없어 점검만.
- ⚠️ 자율프롬프트가 "비로그인 접근정책 A/B/C 미결정"이라 했으나 **이미 결정·구현 완료**(목록공개·상세게이트 ✅). 실제 열린 건 전부 형 대기(법적정보·SQL적용/토큰·수집기 push)지 내 결정사항 아님.

_업데이트: 자율 작업 세션_
